# M2 go/no-go 判决书

- 日期：2026-09-03
- 窗口：2019–2025 赛季（7 个赛季，spec §8.1 要求 ≥5），**11,605 场**（仅含 Pinnacle 收盘 `psc_*` 覆盖场次；窗口内全部 12,459 场中 854 场无收盘价，见「数据说明」）
- 主判决（half-life=100）：**⛔ NO-GO**
- 模型 log-loss **0.99790** vs 市场（去水收盘）**0.96867**（劣化 **+3.02%**，判据 ≤ +1.00% → **超标 3.0 倍**）
- Brier：模型 **0.29776** / 市场 **0.28783**
- 校准：**过度收缩（shrinkage 过强）**——主胜概率在高概率端**低估**（0.7–0.8 十分位：预测 0.740 / 实际 0.782，−0.042），在低概率端**高估**（0.0–0.1：预测 0.083 / 实际 0.054，+0.029）；中段（0.3–0.6）基本贴合。这是模型离散度不足的直接证据：模型 p_home 标准差 **0.148** vs 市场 **0.186**（P5–P95：0.200–0.688 vs 0.147–0.764）。

## 证据链（模型确实有信号，但不够）

| 基准 | log-loss | 相对模型 |
|---|---|---|
| 常数频率基线（全样本 H/D/A 频率） | 1.07449 | 差 +7.7% |
| **本模型（hierarchical DC, hl=100）** | **0.99790** | — |
| Pinnacle 收盘去水 | 0.96867 | 好 −2.9% |

模型显著优于无信息基线（管线拟合、去水、评估链路无缺陷），但**没有跑赢收盘价**。市场自身校准极紧——**所有 n ≥ 100 的十分位** |预测−实际| ≤ 0.027（唯一例外是 0.9–1.0 尾桶：n=13，差 +0.087，属纯二项噪声、无统计意义），证实评估链路与比例法去水无误——差距是真实的模型能力差距，不是 harness 假象。

## 分联赛

| 联赛 | n | 劣化% | 判决 |
|---|---|---|---|
| SP1 | 2468 | +3.28% | NO-GO |
| I1 | 2478 | +3.18% | NO-GO |
| D1 | 1985 | +3.10% | NO-GO |
| E0 | 2490 | +2.92% | NO-GO |
| F1 | 2184 | +2.58% | NO-GO |

五个联赛**全部** NO-GO；最好（F1 +2.58%）也超门槛 2.6 倍。无联赛存在可单独挽救的例外。

## 分赛季

| 赛季 | n | 劣化% | 判决 |
|---|---|---|---|
| 2019 | 1725 | +2.58% | NO-GO |
| 2020 | 1826 | +2.93% | NO-GO |
| 2021 | 1826 | +2.70% | NO-GO |
| 2022 | 1826 | +2.33% | NO-GO |
| 2023 | 1752 | +3.60% | NO-GO |
| 2024 | 1752 | +3.58% | NO-GO |
| 2025 | 898 | +3.87% | NO-GO |

七个赛季**全部** NO-GO，无逐年趋势性改善（2022 年最好 +2.33%，2025 年最差 +3.87%）；劣化不随时间收窄，说明不是「数据太少导致早期差」。

## 模拟盘（收盘价成交，保守）

- 平注：**9,679 注**，ROI **−5.6%**（精确 −5.55%，P&L −537.7 单位）
- ¼ Kelly（本金 1000，单注 ≤2%）：终值 **0.0**（精确 0.0023），ROI **−100.0%**，最大回撤 100.0% —— 爆仓

**参照系（本项目自行补测）**：若不做任何筛选、对 11,605 场的 34,815 个结果全部平注收盘价，ROI = **−3.73%**（即纯水位成本）。模型的 −5.55% **比无脑全押更差**——筛选反而带来负选择。

原因诊断（非缺陷，是 NO-GO 的机理）：门槛几乎不构成筛选。两个必须分开看的口径——

| 口径 | 场次数 | 占 11,605 |
|---|---|---|
| **edge 通过率**：≥1 个结果满足 `edge ≥ 0.02`（仅此一条） | 10,261 | **88.4%** |
| 同上，再加 `EV ≥ 0.03`（未加赔率窗） | 10,011 | 86.3% |
| **实际下注覆盖率**：再加赔率窗 `1.4 ≤ odds ≤ 6.0`（= `candidates()` 全部门槛） | 8,692 | **74.9%** |

即 **74.9% 的场次产生下注**（8,692 场 / 9,679 注，平均 edge 0.066）。88.4% 是 edge 单条通过率，74.9% 才是真实下注覆盖率——两者都不该叫「筛选」。模型相对市场过度收缩，其概率与市场之差的均值为零但方差大（corr(p_home) 仅 0.89），于是「edge」大多是**模型噪声**而非信息增量——按它下注等于在收盘价上做负 EV 的长注偏选（模型平均高估客队 +0.0082、低估主队 −0.0083）。

## 调参对比（supplementary，主判决仍为 half-life=100）

| half-life（天） | 模型 log-loss | 劣化% | 平注 ROI（注数） | 判决 |
|---|---|---|---|---|
| 50 | 1.01096 | +4.37% | −5.9%（10,338） | NO-GO |
| **100（默认，主判决）** | **0.99790** | **+3.02%** | **−5.55%（9,679）** | **NO-GO** |
| 200 | 0.99100 | +2.31% | −6.8%（9,203） | NO-GO |
| 400 | 0.98973 | +2.17% | −6.5%（8,957） | NO-GO |

劣化随半衰期拉长单调改善但**快速趋于平台（≈ +2.1%）**：50→100 提升 1.35pp，100→200 提升 0.71pp，200→400 仅 0.14pp。**没有任何合理的 half-life 取值能降到 +1% 门槛以内**（需从 +2.17% 再降一半以上，外推已饱和）。因此 NO-GO 不是超参选择问题，加大历史窗口也无法挽救。半衰期拉长虽改善 log-loss，平注 ROI 反而更差（−5.6% → −6.8%）——log-loss 与盈利在本区间内不同向。

> 诚实声明：以上扫描为稳健性检验，目的在于证明**判据无法通过**；主判决严格采用 spec 默认参数 half_life_days=100 的那一次运行，未做任何朝 GO 方向的调参。

### 复现命令（审计记录，按执行顺序）

```bash
uv run fa init
# 主判决（half-life=100，spec 默认）—— 先逐联赛执行留档，再单次干净全量重跑出最终报告
uv run fa backtest run --from 2019 --to 2025 --leagues E0
uv run fa backtest run --from 2019 --to 2025 --leagues SP1
uv run fa backtest run --from 2019 --to 2025 --leagues D1
uv run fa backtest run --from 2019 --to 2025 --leagues I1
uv run fa backtest run --from 2019 --to 2025 --leagues F1
uv run fa backtest run --from 2019 --to 2025 --leagues E0,SP1,D1,I1,F1            # <- docs/m2-report.md 的来源
uv run fa backtest run --from 2019 --to 2025 --leagues E0,SP1,D1,I1,F1 --no-refit  # 渲染自检，0.9 s
# half-life 稳健性扫描（supplementary；每次整表覆盖 backtest_predictions）
uv run fa backtest run --from 2019 --to 2025 --leagues E0,SP1,D1,I1,F1 --half-life 50
uv run fa backtest run --from 2019 --to 2025 --leagues E0,SP1,D1,I1,F1 --half-life 200
uv run fa backtest run --from 2019 --to 2025 --leagues E0,SP1,D1,I1,F1 --half-life 400
# 收尾：以默认 half-life=100 再重跑一次，确保提交的 docs/m2-report.md 为主判决口径
uv run fa backtest run --from 2019 --to 2025 --leagues E0,SP1,D1,I1,F1
```

## 数据说明

- `psc_*`（Pinnacle 收盘）实际自 **2012** 赛季起即有覆盖，早于 brief 预估的 2019；`--from 2019` 按计划保留（spec §8.1 walk-forward ≥5 赛季）。
- 2025 赛季（2025-26）仅 **898/1752** 场有收盘价：football-data 源 CSV 的 PSC 列只更新到 **2026-01-04 ～ 2026-01-17**（各联赛截止日不同），其后场次 PSC 空白。为**上游源数据缺口**，非解析/入库缺陷（已逐行核验 `raw_line` 中 PSC 字段本身为空）。854 场缺口几乎全部集中于此。
- 写入行数与可用收盘场次数**逐联赛逐赛季完全相等**（35/35 组），无一场因训练样本 <30 或拟合失败被静默丢弃；共 **1,108** 次 league-week 拟合（1,195 个 league-week 中 87 个无可下注行/无训练数据）。
- F1 2019 仅 279 场为真实历史（2019-20 法甲因疫情提前终止），非数据缺失。

## 性能（wall-clock，M2 实测）

| 阶段 | 耗时 |
|---|---|
| E0（7 赛季，2490 行） | 14.7 s |
| SP1（7 赛季，2468 行） | 15.6 s |
| D1（7 赛季，1985 行） | 10.1 s |
| I1（7 赛季，2478 行） | 12.6 s |
| F1（7 赛季，2184 行） | 13.0 s |
| 全量五联赛单命令（11,605 行，1,108 次拟合） | **62.8 s** |
| `--no-refit` 仅渲染 | 0.9 s |

远低于计划预估的「数分钟至 ~30 分钟」：每次拟合约 55 ms（窗口封顶 1120 天 ≈ 1,000 行 × ~50 队 → 100 参数，L-BFGS 解析梯度收敛极快），ρ 网格并非实际瓶颈。

## 结论

**NO-GO。** 模型 log-loss 相对 Pinnacle 收盘去水劣化 +3.02%，超 spec §8.2 的 +1% 判据 3.0 倍；五联赛、七赛季、四个 half-life 取值下**无一达到 GO**。机理明确：分层 DC + MAP 收缩产生的概率离散度系统性低于市场（过度收缩），在收盘价上无信息增量，模拟盘确认负 EV（平注 −5.6%，¼ Kelly 爆仓）。

**按 spec §10 M2 与 §1「跑不赢市场就止步」：项目止步于研究结论，M3+（persona、实时盘、下单）不启动。**

可留档的研究发现（若未来重启）：模型确有信号（优于常数基线 7.7%），主要缺口是概率锐度而非方向——后续若重启，优先方向是放宽 σ_att/σ_dfn 先验、缩短窗口（half-life 200–400 已示 +2.2% 平台）、以及对 1X3 以外市场（大小球/BTTS，本次因缺收盘赔率未入模拟盘）单独评估。
