# M1 验收报告

- 日期：2026-09-03
- 数据源：football-data.co.uk（`https://www.football-data.co.uk/mmz4281/<赛季码>/<联赛>.csv`）
- 环境：worktree `worktree-m1-plan`，库文件 `data/fa.db`（本次验收前已删除重建）

## 验收结论（spec §10 M1 四条）

| 验收标准 | 结果 |
| --- | --- |
| 五大联赛全量入库 | ✔ 5 联赛 × 34 赛季，共 **59,176** 场，`file_errors` 0 |
| sync-history 幂等 | ✔ 立即重跑「新入库 0 场」，170 个赛季全部内容哈希命中跳过 |
| 抽样 50 场与 CSV 对账一致 | ✔ `对账通过：抽样 50 场，0 不一致` |
| 未知队名 0（隔离表机制） | ✔ `unknown_names` 0 条；隔离机制由 Task 5 单测覆盖 |

## 全量入库（Step 1）

`rm -f data/fa.db data/fa.db-wal data/fa.db-shm && uv run fa init && uv run fa data sync-history`

```
完成：下载 170 个赛季文件，新入库 59176 场，跳过（内容未变）0 个赛季，无数据 0 个
real	0m9.650s   （CSV 已在本地缓存 data/csv/，170 个文件）
```

冷缓存首跑（真正的下载路径）实测 3m34s，当时 9 份文件因解析报错被记账
（见下节「真实数据边角问题」，修复后重跑 0 错误）。

入库分布：

| 联赛 | 赛季数 | 场次 |
| --- | --- | --- |
| E0（英超） | 34（1993-2026） | 12,724 |
| SP1（西甲） | 34 | 12,734 |
| D1（德甲） | 34 | 10,107 |
| I1（意甲） | 34 | 11,746 |
| F1（法甲） | 34 | 11,865 |
| **合计** | **170** | **59,176** |

- 队伍注册 246 支；`team_aliases` 0 条（M1 数据源单一，别名字典在 M3 接入
  Odds API 后才有输入）；`unknown_names` 0 条
- 日期范围 1993-07-23 ~ 2026-08-31；`date IS NULL` 0 条
- 场次/赛季（1993-2025 已完赛赛季平均）：E0 385.0 / SP1 385.0 / D1 306.0 /
  I1 355.3 / F1 359.0

## 幂等验证（Step 2）

`uv run fa data sync-history`（立即重跑）

```
完成：下载 170 个赛季文件，新入库 0 场，跳过（内容未变）170 个赛季，无数据 0 个
real	0m7.295s
```

新入库 0 场 ✔——spec「sync-history 幂等」达成（分区替换 + 整行内容哈希）。

## 对账抽样（Step 3）

`uv run fa data audit --sample 50`

```
对账通过：抽样 50 场，0 不一致
```

比对字段：`fthg / ftag / ps_home / ps_draw / ps_away / psc_home / psc_draw / psc_away`。

**有效性自查（避免「空对空」通过）**：该 50 场样本里有 13 场带 Pinnacle
赛前/收盘价、27 场带射门数据，赔率比对并非全部 NULL==NULL；另以 `--seed 7`
抽 200 场复核，同样 0 不一致（其中 77 场带 Pinnacle 价）。

再以注入式变异验证 audit 确实能抓错（在 DB 副本上人为改 2 个字段后删掉其余
行，迫使抽样命中）：

```
[不一致] match=7385 field=fthg db=1 csv=0
[不一致] match=59005 field=psc_home db=9.99 csv=1.15
共 2 处不一致（抽样 50 场）   （exit 1）
```

即 audit 具备检错能力，正式库的「0 不一致」是真实结论。

## 概况检查（Step 4）

`uv run fa data status`

```
D1: 34 个赛季，10107 场
E0: 34 个赛季，12724 场
F1: 34 个赛季，11865 场
I1: 34 个赛季，11746 场
SP1: 34 个赛季，12734 场
未知队名（隔离表）：0 条
```

**赛季数为 34 而非 25–33 的原因**：验收执行日为 2026-09-03，2026-27 赛季已于
8 月开赛，`SEASONS_FROM=1993` 起算到当前赛季即为 34 个；其中 2026-27 为进行中
赛季，仅含已赛部分（E0 20 / SP1 30 / I1 20 / F1 18 / D1 9，共 97 场）。这与
「每日更新完赛」（spec §3.4，M3 由 hermes cron 调度）的行为一致，属预期。

非典型场次均为真实赛制事实，非入库缺陷：

- E0 1993-94、1994-95 = 462 场，SP1 1995-96、1996-97 = 462 场：当年 22 队
  （42 轮 = 462），正确
- D1 全部赛季 = 306 场：德甲 18 队（34 轮 = 306），正确
- I1 早期赛季 = 306 场：意甲 18 队时期，正确
- F1 2019-20 = 279 场：法甲因新冠疫情 2020-03-08 后中止未复赛，源数据即 279

已完赛赛季（1993-2025）每个最少 279 场，无被截断的赛季。

## 无数据赛季清单

`missing = 0`：1993-2026 的 5 联赛 × 34 赛季全部有数据，football-data.co.uk
对五大联赛自 1993-94 起均有存档（未出现预估中的 90 年代 404）。
`rows_no_date = 0`（未出现垃圾日期行）。`file_errors = 0`。

## 真实数据边角问题与修复（TDD）

首次全量拉取暴露 9 份文件入库失败（其余 161 份不受影响，单文件容错按预期工作）：

```
E0 2003-04 / E0 2004-05 / SP1 2004-05 / D1 2002-03 / D1 2003-04
I1 2003-04 / I1 2004-05 / F1 2002-03 / F1 2003-04
  ParserError: Error tokenizing data. C error: Expected N fields in line M, saw K
```

定位：这些赛季的部分数据行行尾多出若干逗号，字段数比表头还长且多出的**全为
空**。扫描全部 170 个缓存文件：868 行属「尾部多余空字段」类，**0 例**多出非空
字段（另有 12,881 行字段数少于表头，pandas 本就容忍、填 NaN，无需处理）。

修复（commit `a4a9726`）：`parse_csv` 读入前先 `_trim_ragged_lines` 截掉行尾
多余空字段——仅在「多出部分全为空」时截断；带非空值的多余字段维持 pandas 报错
上抛（由 sync 记账），不静默吞数据。先补 3 个失败测试再修：

1. 尾部多余空字段的行可解析且各列取值正确
2. 列数与表头一致的文件逐字节原样返回（裁剪不误伤）
3. 非空多余字段仍抛 `ParserError`（保持严格性）

修复后逐项核验：170 个文件全部可解析；被截断行解析出的 `raw` 字典键数恰等于
表头字段数（E0 2003-04 Tottenham 0-1 Chelsea 为 57 键，B365AH=0.5 等
值逐列对齐），全库 246 支队伍名无一被行尾 `\r` 污染（缓存文件均为 CRLF）。

## 全量测试（Step 6）

`uv run pytest`

```
43 passed in 0.45s
```

40 个既有用例 + 本次新增 3 个解析用例，全绿，且均为离线用例。

## 提交

- `a4a9726` fix: 容忍 CSV 数据行尾部多余空字段（2002-05 五联赛 9 份文件 868 行）
- `a433463` chore: M1 验收报告（全量入库 + 幂等 + 对账 50 场通过）
- `808cacf` fix(test): 恢复坏日期行保留语义的断言（修复提交中误删）
- `8143fcd` fix: audit 复用 csv_cache_path；ingest commit 入 try + 回滚保根因（终审修复）

终审（全分支）结论：**Ready to merge**，0 Critical；最终测试 45 passed（离线）。

## 计划偏差与裁决摘要（M2 规划携带项）

实施中发现并修复 **7 处计划/仓库缺陷**（计划文档未回改，以代码与 git 史为准）：

1. Task 1：typer 单命令折叠——补 `@app.callback()`
2. Task 3：brief 测试的 monkeypatch 顺序 bug——两行对调
3. Task 5：`resolve_team` 别名分支列名笔误——`SELECT id AS team_id`
4. Task 6：采样内容哈希对非采样列修正不收敛——改整行 raw JSON 指纹
5. Task 7：brief 测试放行条件时间炸弹——改精确放行 `(E0, 1995)`
6. Task 9：audit 夹具文件名违反缓存命名契约——`E0_9596.csv`
7. 仓库初始提交：`.gitignore` 的 `data/` 未锚定（会静默排除 `src/fa/data/`）——锚定 `/data/`

另：pandas 钉 `>=2.0,<3`（规避 3.x string dtype 破坏 NaN 处理语义）；uv.lock 内嵌
tsinghua 镜像（单机项目，不处理）。

**M2 携带项**（终审 triage，全部 DEFER-OK，择机处理）：

- `MatchWeek.end` 语义 = 桶内**最后一场比赛日**，不是周三窗口边界——M2 消费时勿误读
- M2 开场建议：`_read_text` 从 sync 提升为共享模块（audit 的私有导入）
- walkforward `ORDER BY date` 无 tiebreaker → 改 `ORDER BY date, id`
- `SCHEMA_VERSION` 升 2 时补版本不匹配路径测试（届时该守卫才承重）
- 内容哈希存于 `meta` 按 `(league, season)`——DB 侧直改数据且哈希匹配是 sync 的
  盲区，由 audit 把关（设计如此）
- M3：`training_matches` 需补「已完赛」谓词（引入未赛 fixture 数据时）；队名别名
  字典在接入 Odds API 后才有输入

