# retro Stage 2（paper_t1）+ 关卡 3（分层检验）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地复盘归因线的 Stage 2（paper_t1 选择器 + 挂 daily 次日流程 + 日报归因段落）与关卡 3（`fa retro analyze` 按 miss_tags 分层的预测效度检验）。

**Architecture:** 选择器新增 `select_paper_t1`：按 kickoff 日期取当日 recommendations 所涉 fixtures，用与 `paper._paired_match` 同模式的查询回连 `matches`，再 join `backtest_predictions` 得到与现有 selector 同形状的 cands。daily 日课在结算后调它跑 `run_retro_batch`（hermes 逐场降级、批异常只记账）。关卡 3 在 analyze.py 里把 retro_attributions（audit 违规行默认剔除——§15 裁定）按标签分层，对单场 log-loss 差做 Mann-Whitney U 双侧检验。

**Tech Stack:** Python 3.11+ / sqlite3 / typer / scipy.stats.mannwhitneyu / pytest。

**Spec:** docs/superpowers/specs/2026-09-04-retro-attribution-design.md（§4 选择器表、§8-3 关卡 3、§9 降级、§10 分期、§11 CLI、§15 实测与裁定行）。

## Global Constraints

- agent 只消费/产出结构化 JSON，不碰数据库；落库由 Python 侧执行（spec §1.4 / §2.2 / 设计 §2）。
- retro 产出**永不进入** B 线推荐流与 paper 落注流；retro 代码对 recommendations/bets/paper **无任何写通路**（设计 §2——本计划只**读** recommendations，这是 Stage 2 的立项前提，不越界）。
- 诚实条款：违规率/失败率/样本量如实输出，不得为出现分层差距而调整口径或剔除行（设计 §8、清单 #9）。
- audit 违规行（evidence 日期不合规）在负责人裁定前**不得作为分层依据**——analyze 默认剔除并显式报 `n_excluded`（设计 §15 实测行）。
- **零 schema 改动**：`retro_attributions`/`retro_runs` 的 selector CHECK 词表建表时已含 `paper_t1`（db.py `_RETRO_TABLE` 注释明示「趁表空一次到位」）。不得 bump SCHEMA_VERSION。
- 单场 hermes 失败只落该场 status，批不中断；retro 批整体异常在 daily 内降级记账，不中断日课（设计 §9）。
- 真实下注依然禁止（spec §7.2）——本计划不触碰任何下单通道。
- 测试不得依赖当前日期（清单 #1 时间炸弹）：`select_paper_t1` 显式收 `target_date`；daily 侧 `_yesterday()` 提为模块函数供 monkeypatch。
- mock 一律「先 patch 后取路径」，patch 目标为**使用方模块属性**（test_daily 既有惯例：`monkeypatch.setattr(daily, "sync_history", ...)`）。

---

### Task 1: `select_paper_t1` 选择器 + CLI 词表扩展

**Files:**
- Modify: `src/fa/retro/select.py`（文件末尾追加）
- Modify: `src/fa/cli.py:649-708`（`retro_run` 加 paper_t1 分支与 `--date` 选项）
- Test: `tests/retro/test_select.py`（追加）

**Interfaces:**
- Consumes: `fa.backtest.metrics.fetch_predictions`（既有）；`build_pack(conn, cand)` 要求 cand 含 `match_id/league/season/date/home/away` 键（export.py 既有契约——fetch_predictions 行 + home/away 即满足）。
- Produces: `select_paper_t1(conn, target_date: str) -> tuple[list[dict], dict]`——返回 `(cands, meta)`，meta = `{"n_fixtures": int, "n_unpaired": int, "n_no_prediction": int}`（进 `retro_runs.params_json` 记账）。cand 行 = fetch_predictions 行 + `home/away/is_control=False`（`is_control` 恒 0：paper_t1 无病例-对照语义，设计 §7）。
- 注意：返回签名是二元组，与 `select_divergence`/`select_manual`（单 list）**不同**——meta 记账是选择器语义的一部分，CLI 分支特判。

- [ ] **Step 1: 写失败测试**（追加到 tests/retro/test_select.py；复用文件内既有 `_seed`）

```python
# ---- paper_t1（Stage 2，设计 §4：前一比赛日 recommendations 所涉场次）----

def _seed_t1(conn):
    """昨日推荐链：fixture（kickoff 2024-04-20）+ recommendation + 完赛对齐。
    复用 _seed 的 teams/matches/backtest_predictions。注意主客 id：_seed 的
    match 1 = Arsenal(id1) vs West Ham(id3)——fixture 必须写 (1,3) 才配到
    match 1（写 (1,2) 会配到 match 3 = Arsenal vs Chelsea，2024-04-22）。"""
    conn.execute(
        "INSERT INTO fixtures (league, event_key, source, kickoff_utc,"
        " home_team_id, away_team_id, status, created_at)"
        " VALUES ('E0','ev-t1','oddsapi','2024-04-20T14:00:00Z',1,3,"
        " 'finished','2024-04-20T08:00:00Z')")          # id 1
    conn.execute(
        "INSERT INTO runs (type, phase, started_at, status)"
        " VALUES ('matchday','am','2026-09-04T03:00:00Z','ok')")   # id 1
    conn.execute(
        "INSERT INTO recommendations (run_id, fixture_id, strategy, market,"
        " phase, model_p, market_p, best_odds, bookmaker, edge, ev,"
        " kelly_stake_frac, created_at)"
        " VALUES (1,1,'model_only','H','am',0.45,0.52,2.10,'Pinnacle',"
        "0.063,0.132,0.05,'2026-09-04T03:05:00Z')")
    conn.commit()


class TestSelectPaperT1:
    def test_paired_recommendation_match_selected(self, conn):
        _seed(conn); _seed_t1(conn)
        cands, meta = select_paper_t1(conn, "2024-04-20")
        assert [c["match_id"] for c in cands] == [1]
        assert meta == {"n_fixtures": 1, "n_unpaired": 0, "n_no_prediction": 0}
        c = cands[0]
        assert c["home"] == "Arsenal" and c["away"] == "West Ham"
        assert c["is_control"] is False
        assert c["outcome"] == "H"            # build_pack 契约键来自 fetch_predictions

    def test_unpaired_fixture_counted_not_fatal(self, conn):
        _seed(conn); _seed_t1(conn)
        # 换成库里无完赛行的两队（Liverpool 主场无 match）
        conn.execute("UPDATE fixtures SET home_team_id=4, away_team_id=3 WHERE id=1")
        conn.commit()
        cands, meta = select_paper_t1(conn, "2024-04-20")
        assert cands == [] and meta["n_unpaired"] == 1

    def test_no_prediction_row_skipped_and_counted(self, conn):
        _seed(conn); _seed_t1(conn)
        conn.execute("DELETE FROM backtest_predictions WHERE match_id=1")
        conn.commit()
        cands, meta = select_paper_t1(conn, "2024-04-20")
        assert cands == [] and meta["n_no_prediction"] == 1

    def test_other_date_selects_nothing(self, conn):
        _seed(conn); _seed_t1(conn)
        cands, meta = select_paper_t1(conn, "2024-04-21")
        assert cands == [] and meta["n_fixtures"] == 0
```

（`conn` fixture 若文件里没有，用 test_paper 同款：`init_db(tmp_path/"t.db")` + `connect`——与文件头「种子直插库」模式一致，先看文件里已有的 fixture 复用之。）

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/retro/test_select.py -k paper_t1 -v`
Expected: FAIL with `ImportError: cannot import name 'select_paper_t1'`

- [ ] **Step 3: 实现**（src/fa/retro/select.py 文件末尾追加）

```python
# ---- paper_t1（Stage 2，设计 §4）-----------------------------------------
# 前一比赛日 recommendations 所涉场次：fixture 按 kickoff UTC 日历日命中，
# 回连 matches 拿赛果与 A 线预测行。配对查询与 paper._paired_match 同模式
# （窗 [kickoff, kickoff+2 天]、(日期差, 日期, id) 最小者）——设计 §15 开放
# 问题同款处置：先独立实现同模式，两边都稳后再提取共享，不跨模块 import
# 私有名。缺 backtest_predictions 行的场次跳过并计数（v1 无该场模型概率
# 则信息集不完整，§5「三方预测」缺一不可——诚实计数，不硬跑）。

def _kick_day(kickoff_utc):
    try:
        return date.fromisoformat(kickoff_utc[:10])
    except (TypeError, ValueError):
        return None


def _pair_match(conn, league, home_id, away_id, kick):
    if kick is None or home_id is None or away_id is None:
        return None
    best, best_key = None, None
    for row in conn.execute(
            "SELECT * FROM matches WHERE league=? AND home_team_id=?"
            " AND away_team_id=? AND fthg IS NOT NULL AND ftag IS NOT NULL"
            " ORDER BY date, id", (league, home_id, away_id)).fetchall():
        try:
            played = date.fromisoformat(row["date"][:10])
        except (TypeError, ValueError):
            continue
        gap = (played - kick).days
        if gap < 0 or gap > 2:               # MAX_MATCH_DAY_GAP 同值（paper.py:54）
            continue
        key = (gap, row["date"], row["id"])
        if best_key is None or key < best_key:
            best, best_key = row, key
    return best


def select_paper_t1(conn, target_date: str) -> tuple[list[dict], dict]:
    """target_date = 比赛日（kickoff UTC 日历日，'YYYY-MM-DD'）。"""
    preds = {r["match_id"]: r for r in fetch_predictions(conn)}
    names = {r["id"]: r["name"] for r in conn.execute(
        "SELECT id, name FROM teams")}
    fixtures = conn.execute(
        "SELECT DISTINCT f.league, f.home_team_id, f.away_team_id,"
        " f.kickoff_utc FROM recommendations r"
        " JOIN fixtures f ON f.id = r.fixture_id"
        " WHERE substr(f.kickoff_utc, 1, 10) = ?", (target_date,)).fetchall()
    meta = {"n_fixtures": len(fixtures), "n_unpaired": 0,
            "n_no_prediction": 0}
    cands, seen = [], set()
    for f in fixtures:
        m = _pair_match(conn, f["league"], f["home_team_id"],
                        f["away_team_id"], _kick_day(f["kickoff_utc"]))
        if m is None:
            meta["n_unpaired"] += 1
            continue
        p = preds.get(m["id"])
        if p is None:
            meta["n_no_prediction"] += 1
            continue
        if m["id"] in seen:                  # 同 match 多 fixture（罕见）只取一次
            continue
        seen.add(m["id"])
        row = dict(p)
        row["home"] = names.get(m["home_team_id"])
        row["away"] = names.get(m["away_team_id"])
        row["is_control"] = False
        cands.append(row)
    cands.sort(key=lambda r: r["match_id"])
    return cands, meta
```

（文件顶部 import 需补 `from datetime import date`。）

- [ ] **Step 4: CLI 分支**（src/fa/cli.py `retro_run`）

函数签名加 `tdate: str = typer.Option("", "--date", help="paper_t1：比赛日 YYYY-MM-DD，空=昨天")`；`retro_run` 内 import 行补 `select_paper_t1`；`elif selector == "manual":` 块之后、`else:` 之前插：

```python
        elif selector == "paper_t1":
            from datetime import date as _d, timedelta as _td
            day = tdate or (_d.today() - _td(days=1)).isoformat()
            cands, t1meta = select_paper_t1(conn, day)
            params = {"date": day, **t1meta, "attributors": attributors}
```

`--selector` 的 help 文案与 else 分支报错文案同步改为 `divergence|manual|paper_t1（agentline_aligned 属 S3）`。

- [ ] **Step 5: 跑全量 + Commit**

Run: `uv run pytest tests/retro/ tests/test_cli.py -q`
Expected: 全 PASS（新增 4 条）

```bash
git add src/fa/retro/select.py src/fa/cli.py tests/retro/test_select.py
git commit -m "feat(retro): select_paper_t1 选择器——昨日推荐场次回连 matches/predictions + CLI 词表"
```

---

### Task 2: `render_retro_brief` 日报归因段落

**Files:**
- Modify: `src/fa/report/render.py:506` 附近（`render_settlement_brief` 之后追加）
- Test: `tests/report/test_render.py`（追加）

**Interfaces:**
- Produces: `render_retro_brief(rows, batch) -> str`——`rows` = sqlite3.Row 列表（键 `date/league/digest/primary_tag/tags_confidence`，来自 `retro_attributions` status='ok' 且 attributor=1 的行），`batch` = `run_retro_batch` 返回摘要（用 `batch_id/n_selected/n_ok`）。空 rows 返回空串（调用方据此不附加）。纯函数，不触 DB。

- [ ] **Step 1: 写失败测试**

```python
class TestRenderRetroBrief:
    def _row(self, **kw):
        base = {"date": "2026-09-03", "league": "E0", "digest": "伤停两名主力，市场已消化。",
                "primary_tag": "injury", "tags_confidence": 0.7}
        base.update(kw)
        return base

    def test_renders_batch_line_and_per_match(self):
        out = render_retro_brief(
            [self._row()], {"batch_id": 9, "n_selected": 2, "n_ok": 1})
        assert "复盘归因" in out and "批 #9" in out
        assert "2026-09-03 E0" in out and "injury" in out and "0.70" in out
        assert "伤停两名主力" in out            # digest 摘录

    def test_long_digest_truncated(self):
        out = render_retro_brief(
            [self._row(digest="长" * 200)],
            {"batch_id": 9, "n_selected": 1, "n_ok": 1})
        assert "长" * 40 in out and "长" * 41 not in out   # 截到 40 字

    def test_empty_rows_returns_empty_string(self):
        assert render_retro_brief(
            [], {"batch_id": 9, "n_selected": 0, "n_ok": 0}) == ""
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/report/test_render.py -k retro_brief -v`
Expected: FAIL with `ImportError: cannot import name 'render_retro_brief'`

- [ ] **Step 3: 实现**（render.py，`render_settlement_brief` 函数后追加）

```python
def render_retro_brief(rows, batch):
    """daily 简报的复盘归因段落（Stage 2，设计 §3「日报摘要段落」）。

    rows 为空返回空串——调用方据此不附加（昨日无可归因场次不是事件）。
    digest 截 40 字：段落是摘要不是全文，全文在 retro_attributions。
    """
    if not rows:
        return ""
    lines = [f"复盘归因（paper T+1，批 #{batch['batch_id']}）："
             f"昨日推荐 {batch['n_selected']} 场，归因 ok {batch['n_ok']} 场"]
    for r in rows:
        digest = (r["digest"] or "")[:40]
        lines.append(f"  {r['date']} {r['league']} {r['primary_tag']}"
                     f"（conf {r['tags_confidence']:.2f}）：{digest}")
    parts = [f"{t}×{n}" for t, n in sorted(
        Counter(r["primary_tag"] for r in rows).items())]
    lines.append("  标签分布：" + "，".join(parts))
    return "\n".join(lines)
```

（`from collections import Counter` 提到文件顶部 import 区。rows 非空 ⇒ status='ok' 行的 primary_tag 恒非空 ⇒ Counter 恒非空，无需兜底分支。）

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/report/test_render.py -q`
Expected: 全 PASS（新增 3 条）

- [ ] **Step 5: Commit**

```bash
git add src/fa/report/render.py tests/report/test_render.py
git commit -m "feat(report): render_retro_brief 日报归因段落（批摘要+逐场主标签+digest 40 字截断）"
```

---

### Task 3: `run_daily` 挂载 paper_t1 + 推送拼接

**Files:**
- Modify: `src/fa/pipeline/daily.py`（`_run` 内 settle 之后、推送之前）
- Modify: `src/fa/cli.py:838` 附近（`run_daily_cmd` 输出补一行）
- Test: `tests/pipeline/test_daily.py`（既有用例核对 + 新增 3 条）

**Interfaces:**
- Consumes: Task 1 `select_paper_t1`、既有 `run_retro_batch(conn, cands, selector, params, out_root)`（attributors 默认 1）、Task 2 `render_retro_brief(rows, batch)`。
- Produces: `run_daily` 返回摘要新增键 `"retro"`（`run_retro_batch` 摘要 dict 或 None=昨日无场次/未跑）与 `"retro_error"`（str 或 None）。模块新增 `_yesterday() -> str`（供 monkeypatch，防时间炸弹）。推送文本 = `render_settlement_brief(settle) + "\n" + retro 段`（段非空才拼）。

- [ ] **Step 1: 写失败测试**（追加到 tests/pipeline/test_daily.py；沿用既有 env fixture 与种子辅助）

```python
# ---- Stage 2：paper_t1 复盘挂载（设计 §10）----

def _seed_yesterday_rec(conn):
    """昨日推荐链（Task 1 同构）：自带 teams/runs 种子；**fixture 与
    recommendation/matches 的 id 全用 lastrowid**——调用方可能先跑过
    _seed_settleable（其 teams/matches/fixtures 各占 id 1），字面量 id 会
    错位挂到他行。kickoff/比赛日/推荐时间线 = 2024-09-02。"""
    conn.executemany(
        "INSERT OR IGNORE INTO teams (league, name) VALUES (?, ?)",
        [("E0", "Arsenal"), ("E0", "Chelsea")])
    rid = conn.execute(
        "INSERT INTO runs (type, phase, started_at, status)"
        " VALUES ('matchday','am','2024-09-02T03:00:00Z','ok')").lastrowid
    h, a = 1, 2                       # teams 前两行（OR IGNORE 幂等保持 1/2）
    mid = conn.execute(
        "INSERT INTO matches (league, season, date, home_team_id,"
        " away_team_id, fthg, ftag, raw_line) VALUES"
        " ('E0', 2024, '2024-09-02', ?, ?, 2, 0, '{}')", (h, a)).lastrowid
    conn.execute(
        "INSERT INTO backtest_predictions (league, season, week_index,"
        " match_id, date, p_home, p_draw, p_away, mkt_home, mkt_draw,"
        " mkt_away, outcome) VALUES"
        " ('E0', 2024, 1, ?, '2024-09-02', 0.45, 0.28, 0.27,"
        " 0.52, 0.26, 0.22, 'H')", (mid,))
    fid = conn.execute(
        "INSERT INTO fixtures (league, event_key, source, kickoff_utc,"
        " home_team_id, away_team_id, status, created_at)"
        " VALUES ('E0','ev-r1','oddsapi','2024-09-02T14:00:00Z',?,?,"
        " 'finished','2024-09-02T08:00:00Z')", (h, a)).lastrowid
    conn.execute(
        "INSERT INTO recommendations (run_id, fixture_id, strategy, market,"
        " phase, model_p, market_p, best_odds, bookmaker, edge, ev,"
        " kelly_stake_frac, created_at)"
        " VALUES (?,?, 'model_only','H','am',0.45,0.52,2.10,'Pinnacle',"
        "0.063,0.132,0.05,'2024-09-02T03:05:00Z')", (rid, fid))
    conn.commit()


class TestDailyRetro:
    def _fake_batch(self, conn, n_ok=1):
        """替身批：返回合法摘要并落一条 ok 归因行（简报段落有内容可渲染）。"""
        def fake(conn_, cands, selector, params, out_root, attributors=1):
            rid = conn_.execute(
                "INSERT INTO retro_runs (selector, params_json, n_selected,"
                " n_ok, n_parse_fail, n_timeout, n_error, duration_s,"
                " created_at) VALUES ('paper_t1','{}',?, ?,0,0,0,0.5,"
                "'2026-09-04T06:31:00Z')",
                (len(cands), n_ok)).lastrowid
            conn_.execute(
                "INSERT INTO retro_attributions (batch_id, match_id, league,"
                " season, date, selector, attributor, miss_tags_json,"
                " primary_tag, tags_confidence, model_vs_market,"
                " evidence_json, digest, status, repaired, harness, model,"
                " duration_s, input_pack_path, tag_set_version, created_at,"
                " is_control) VALUES (?,?, 'E0',2024,'2024-09-02','paper_t1',"
                "1,'[\"injury\"]','injury',0.7,'model_wrong','[]',"
                "'伤停主力中卫。','ok',0,'hermes',NULL,11.8,'p.json','v1',"
                "'2026-09-04T06:31:10Z',0)",
                (rid, cands[0]["match_id"]))
            conn_.commit()
            return {"batch_id": rid, "n_selected": len(cands), "n_ok": n_ok,
                    "n_parse_fail": 0, "n_timeout": 0, "n_error": 0,
                    "duration_s": 0.5}
        return fake

    def test_retro_runs_after_settle_and_appends_brief(self, env):
        # 前提：推送只在 settled>0 时发生（daily 静默规则）——先落一支可结注
        # （既有 _seed_settleable：kickoff 2026-09-02 的 fixture + pending bet
        # + 完赛 match），再叠昨日推荐链。_yesterday 被 patch 成 '2024-09-02'，
        # 与 settleable 的 2026-09-02 fixture 不撞——select_paper_t1 只选
        # 2024-09-02 那条。
        _seed_settleable(env.conn)
        _seed_yesterday_rec(env.conn)
        calls = []
        real = self._fake_batch(env.conn)
        def spy(conn_, cands, selector, params, out_root, attributors=1):
            env.order.append("retro")          # 记录时点，钉「结算在前」顺序
            calls.append((selector, params["date"], len(cands)))
            return real(conn_, cands, selector, params, out_root, attributors)
        env.monkeypatch.setattr(daily, "_yesterday", lambda: "2024-09-02")
        env.monkeypatch.setattr(daily, "run_retro_batch", spy)
        out = daily.run_daily(env.conn)
        assert calls == [("paper_t1", "2024-09-02", 1)]
        assert out["retro"]["n_ok"] == 1 and out["retro_error"] is None
        assert "复盘归因" in env.pushed[0] and "injury" in env.pushed[0]
        assert env.order.index("settle") < env.order.index("retro")

    def test_no_yesterday_recs_skips_retro(self, env):
        def boom(*a, **k):
            raise AssertionError("无昨日推荐不得跑批")
        env.monkeypatch.setattr(daily, "_yesterday", lambda: "2024-09-02")
        env.monkeypatch.setattr(daily, "run_retro_batch", boom)
        out = daily.run_daily(env.conn)
        assert out["retro"] is None and out["retro_error"] is None

    def test_retro_batch_error_degrades_not_fatal(self, env):
        _seed_yesterday_rec(env.conn)
        def boom(*a, **k):
            raise RuntimeError("hermes down")
        env.monkeypatch.setattr(daily, "_yesterday", lambda: "2024-09-02")
        env.monkeypatch.setattr(daily, "run_retro_batch", boom)
        out = daily.run_daily(env.conn)          # 不抛 = 降级成功
        assert "hermes down" in out["retro_error"] and out["retro"] is None
```

（既有 `env` fixture 的 SimpleNamespace 未挂 monkeypatch——在 fixture 的 `box = SimpleNamespace(...)` 行补一个键 `monkeypatch=monkeypatch`（既有用例零影响），上述用例即可用 `env.monkeypatch.setattr(...)`。若读现有实现发现已挂，则删此括号直接用。）

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/pipeline/test_daily.py -k DailyRetro -v`
Expected: FAIL with `AttributeError: module 'fa.pipeline.daily' has no attribute '_yesterday'`

- [ ] **Step 3: 实现**（src/fa/pipeline/daily.py）

顶部 import 区追加：

```python
from fa.config import project_root
from fa.report.render import render_retro_brief
from fa.retro.pipeline import run_retro_batch
from fa.retro.select import select_paper_t1


def _yesterday() -> str:
    """昨日（kickoff 日历日）——独立函数供测试 monkeypatch（防时间炸弹）。"""
    from datetime import date, timedelta
    return (date.today() - timedelta(days=1)).isoformat()
```

`_run` 内、`settle = settle_paper_bets(conn)` 之后插（`sync_degraded` 判定之前或之后均可，紧邻 settle 语义最清晰）：

```python
    # Stage 2：paper T+1 复盘（设计 §4/§10）——sync 已把完赛行入库，昨日推荐
    # 场次可归因。选场为空则静默跳过（非事件）；批整体异常只记账不中断日课
    # （§9——单场失败在批内逐场降级，这里兜的是编排层异常）。attributors=1
    # （v1 单跑；ensemble 属 A_multi 侧演化，daily 不上量）。
    retro = None
    retro_error = None
    try:
        day = _yesterday()
        t1c, t1meta = select_paper_t1(conn, day)
        if t1c:
            retro = run_retro_batch(
                conn, t1c, "paper_t1", {"date": day, **t1meta},
                project_root() / "data" / "retro" / "inputs")
    except Exception as exc:
        retro = None
        retro_error = f"{type(exc).__name__}: {exc}"
```

`summary` dict 补两键：`"retro": retro, "retro_error": retro_error`。推送拼接处（`if settle["settled"] > 0:` 块内）改为：

```python
        brief = render_settlement_brief(settle)
        if retro is not None and retro["n_ok"]:
            rows = conn.execute(
                "SELECT date, league, digest, primary_tag, tags_confidence"
                " FROM retro_attributions WHERE batch_id=? AND status='ok'"
                " AND attributor=1 ORDER BY date, match_id",
                (retro["batch_id"],)).fetchall()
            brief += "\n" + render_retro_brief(rows, retro)
        sent = send(brief)
```

`run_daily_cmd`（cli.py）在结算行后补：

```python
    if out["retro"] is not None:
        typer.echo(f"  复盘归因：批 #{out['retro']['batch_id']}，"
                   f"ok {out['retro']['n_ok']}/{out['retro']['n_selected']}")
    elif out["retro_error"]:
        typer.echo(f"  复盘归因：降级（{out['retro_error']}）")
```

- [ ] **Step 4: 跑全量（含既有用例核对）**

Run: `uv run pytest tests/pipeline/ tests/retro/ -q`
Expected: 全 PASS。**既有用例核对（已知两处会受影响，实施时一并修）**：
1. `test_settled_bets_push_brief_and_record_daily_run` 断言 `env.order == ["sync", "settle"]` 全等——它未 patch `_yesterday`，生产实现会算真实 today-1；虽今日跑不撞库内 2026-09-02 fixture（隐性时间炸弹），须显式钉死前提：用例内加 `env.monkeypatch.setattr(daily, "_yesterday", lambda: "1999-01-01")`（该日期库内无任何 fixture → retro 必跳过 → order 不变）。同文件内其他带 `env.order == [...]` 全等断言的用例同法处理。
2. 若有对 `summary` 整 dict 全等的断言（如 `summary_of(c, ...)` 的 `==` 比对），为期望值补 `"retro": None, "retro_error": None` 两键——不改生产行为。

- [ ] **Step 5: Commit**

```bash
git add src/fa/pipeline/daily.py src/fa/cli.py tests/pipeline/test_daily.py
git commit -m "feat(daily): 挂载 paper_t1 复盘步——结算后归因昨日推荐场次+简报拼段，批异常降级不中断"
```

---

### Task 4: `fa retro analyze` 分层检验（关卡 3）+ 设计档写回

**Files:**
- Modify: `src/fa/retro/analyze.py`（`audit_batch` 判定核心提取为 `_row_violation` + 新增 `stratified_analysis`/`render_analysis_report`）
- Modify: `src/fa/cli.py`（retro 组加 `analyze` 命令）
- Modify: `docs/superpowers/specs/2026-09-04-retro-attribution-design.md`（§8-3 方法写回、§11 CLI 表、§15 开放问题闭环）
- Test: `tests/retro/test_cli_retro.py`（追加 CLI 用例）

**Interfaces:**
- Consumes: `PREMATCH_CAUSE_TAGS`/`TAGS`（contract.py 既有）；`fetch_predictions`、`select._divergence`（复用单场 log-loss 差定义——两处定义必须同源，见清单 #6：analyze 不自写公式，直接 `from fa.retro.select import _divergence`）。
- Produces: `_row_violation(tags, evidence_json, match_date) -> str | None`（None=合规；str=违规原因——audit 与 analyze 共用单一事实源）；`stratified_analysis(conn, batch_id=None, include_violations=False) -> dict`；`render_analysis_report(res) -> str`；CLI `fa retro analyze [--batch-id N] [--include-violations]`。

- [ ] **Step 1: 写失败测试**（追加到 tests/retro/test_cli_retro.py；造数沿用该文件既有辅助——先读文件顶部的库/表种子函数复用）

```python
# ---- 关卡 3：分层检验（设计 §8-3）----

class TestAnalyze:
    def _seed_attrib(self, conn, match_id, tags, evidence="[]",
                     date_="2024-04-20", is_control=0, batch_id=1):
        conn.execute(
            "INSERT INTO retro_attributions (batch_id, match_id, league,"
            " season, date, selector, attributor, miss_tags_json,"
            " primary_tag, tags_confidence, model_vs_market,"
            " evidence_json, digest, status, repaired, harness, model,"
            " duration_s, input_pack_path, tag_set_version, created_at,"
            " is_control) VALUES (?,?, 'E0',2024,?, 'manual',1,?,"
            " ?,0.7,'model_wrong',?,'d','ok',0,'hermes',NULL,1.0,'p',"
            " 'v1','2026-09-04T00:00:00Z',?)",
            (batch_id, match_id, date_, json.dumps(tags), tags[0],
             evidence, is_control))
        conn.commit()

    def test_stratified_point_estimates_handchecked(self, conn):
        """手算口径：test_select._seed 三场 div = +0.1446 / −0.2049 / −0.0741。
        归因行：match1 标 injury（div 0.1446），match2/3 无 injury 标签
        （div −0.2049/−0.0741）→ injury 层均值 0.1446，对照层均值
        (−0.2049 + −0.0741)/2 = −0.1395。"""
        _seed(conn)
        for mid, tags in [(1, ["injury"]), (2, ["variance"]),
                          (3, ["variance"])]:
            self._seed_attrib(conn, mid, tags)
        res = stratified_analysis(conn)
        t = res["per_tag"]["injury"]
        assert t["n"] == 1 and t["mean_div"] == pytest.approx(0.1446, abs=1e-4)
        assert t["mean_div_rest"] == pytest.approx(-0.1395, abs=1e-4)
        v = res["per_tag"]["variance"]
        assert v["n"] == 2 and v["n_rest"] == 1
        assert res["n_excluded"] == 0

    def test_violating_rows_excluded_by_default(self, conn):
        _seed(conn)
        self._seed_attrib(conn, 1, ["injury"],
                          evidence=json.dumps(
                              [{"title": "t", "date": "2024-04-21",
                                "url": "u"}]))          # 不早于比赛日 → 违规
        self._seed_attrib(conn, 2, ["variance"])
        res = stratified_analysis(conn)                  # 默认剔除
        assert res["n_excluded"] == 1
        assert res["per_tag"]["injury"]["n"] == 0
        res2 = stratified_analysis(conn, include_violations=True)
        assert res2["n_excluded"] == 0
        assert res2["per_tag"]["injury"]["n"] == 1

    def test_case_control_counts_split(self, conn):
        _seed(conn)
        self._seed_attrib(conn, 1, ["injury"], is_control=0)
        self._seed_attrib(conn, 2, ["injury"], is_control=1)
        t = stratified_analysis(conn)["per_tag"]["injury"]
        assert (t["n"], t["n_case"], t["n_control"]) == (2, 1, 1)

    def test_mwu_small_layer_gives_none_or_pvalue(self, conn):
        """n<2 的层无法检验——p_value=None，不抛错（小样本如实降格）。"""
        _seed(conn)
        self._seed_attrib(conn, 1, ["injury"])
        res = stratified_analysis(conn)
        assert res["per_tag"]["injury"]["p_value"] is None

    def test_cli_analyze_outputs_tag_lines(self, cli_runner, conn):
        _seed(conn)
        self._seed_attrib(conn, 1, ["injury"])
        result = cli_runner.invoke(app, ["retro", "analyze"])
        assert result.exit_code == 0
        assert "injury" in result.output and "分层" in result.output
```

（`_seed`/`cli_runner`/`conn` 若与 test_cli_retro.py 既有辅助不同名，改用该文件的实际辅助名；`stratified_analysis` 从 `fa.retro.analyze` import。）

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/retro/test_cli_retro.py -k Analyze -v`
Expected: FAIL with `ImportError: cannot import name 'stratified_analysis'`

- [ ] **Step 3: 实现**（src/fa/retro/analyze.py）

文件顶部 import 补 `from fa.retro.contract import PREMATCH_CAUSE_TAGS, TAGS`（PREMATCH_CAUSE_TAGS 已有则只补 TAGS）、`from fa.retro.select import _divergence`、`from fa.backtest.metrics import fetch_predictions`。`audit_batch` 循环体的证据判定替换为共用函数（行为不变——checked 计数仍在交集判定之后）：

```python
def _row_violation(tags: set, evidence_json, match_date: str):
    """有赛前成因标签的行：证据非空且全部 date 可解析、早于比赛日。

    None=合规/不适用标签集；str=违规原因。audit 与 analyze 共用
    （§15 裁定行：违规行不得作分层依据——单一事实源，两处判定不得分叉）。
    """
    if not tags & PREMATCH_CAUSE_TAGS:
        return None
    ev = json.loads(evidence_json or "[]")
    if not ev:
        return "零证据"
    for e in ev:
        d = str(e.get("date", ""))[:10]
        try:
            date.fromisoformat(d)
        except ValueError:
            return f"证据日期不可解析: {e.get('date')!r}"
        if d >= match_date:
            return f"证据日期 {e.get('date')!r} 不早于比赛日"
    return None
```

`audit_batch` 内循环改为：

```python
    for r in conn.execute(sql, args):
        tags = set(json.loads(r["miss_tags_json"]))
        if not tags & PREMATCH_CAUSE_TAGS:
            continue                      # 非赛前成因标签：无日期规则
        checked += 1
        v = _row_violation(tags, r["evidence_json"], r["date"])
        if v:
            violations.append((r["id"], r["match_id"], v))
```

文件末尾追加：

```python
def _mwu(a: list[float], b: list[float]):
    """Mann-Whitney U 双侧；任一侧 n<2 无法检验返回 None；n<8 用 exact。"""
    if len(a) < 2 or len(b) < 2:
        return None
    from scipy.stats import mannwhitneyu
    method = "exact" if (len(a) < 8 or len(b) < 8) else "asymptotic"
    return float(mannwhitneyu(a, b, alternative="two-sided",
                              method=method).pvalue)


def _mean(xs):
    return sum(xs) / len(xs) if xs else None


def stratified_analysis(conn: sqlite3.Connection, batch_id=None,
                        include_violations: bool = False) -> dict:
    """关卡 3（设计 §8-3）：按 miss_tags 分层，比标签层 vs 无标签层的
    单场 log-loss 差（div，复用 select._divergence 同源定义）。

    代表行：每 (batch_id, match_id) 取聚合行（attributor=0）优先，无聚合行
    取单成员行（attributor=1）——ensemble 批不被成员行重复计数。audit 违规
    行默认剔除（§15 裁定前不作分层依据），--include-violations 可含并显式
    报 n_excluded。缺 backtest_predictions 的场次跳过计数（div 无从算起）。
    诚实条款：不因「想让某层显著」而调整口径；点估计+样本量为主读数，
    p 值仅参考（v1 小样本不装精确）。
    """
    sql = ("SELECT a.batch_id, a.match_id, a.date, a.miss_tags_json,"
           " a.evidence_json, a.is_control FROM retro_attributions a"
           " WHERE a.status='ok' AND (a.attributor=0 OR (a.attributor=1"
           " AND NOT EXISTS (SELECT 1 FROM retro_attributions b"
           " WHERE b.batch_id=a.batch_id AND b.match_id=a.match_id"
           " AND b.attributor=0 AND b.status='ok')))")
    args: list = []
    if batch_id is not None:
        sql += " AND a.batch_id=?"
        args.append(batch_id)
    preds = {r["match_id"]: r for r in fetch_predictions(conn)}
    rows, n_excluded, n_no_pred = [], 0, 0
    for r in conn.execute(sql, args):
        tags = set(json.loads(r["miss_tags_json"]))
        if not include_violations and _row_violation(
                tags, r["evidence_json"], r["date"]):
            n_excluded += 1
            continue
        p = preds.get(r["match_id"])
        if p is None:
            n_no_pred += 1
            continue
        rows.append({"tags": tags, "div": _divergence(p),
                     "is_control": r["is_control"]})
    per_tag = {}
    for t in TAGS:
        layer = [r for r in rows if t in r["tags"]]
        rest = [r for r in rows if t not in r["tags"]]
        per_tag[t] = {
            "n": len(layer),
            "n_case": sum(1 for r in layer if not r["is_control"]),
            "n_control": sum(1 for r in layer if r["is_control"]),
            "mean_div": _mean([r["div"] for r in layer]),
            "n_rest": len(rest),
            "mean_div_rest": _mean([r["div"] for r in rest]),
            "p_value": _mwu([r["div"] for r in layer],
                            [r["div"] for r in rest]),
        }
    return {"n_rows": len(rows), "n_excluded": n_excluded,
            "n_no_prediction": n_no_pred, "per_tag": per_tag,
            "method": "Mann-Whitney U 双侧；单元=单场 log-loss 差；"
                      "任一侧 n<8 用 exact；p 值仅参考（点估计+样本量为主）"}


def render_analysis_report(res: dict) -> str:
    lines = [f"标签分层效度检验（n={res['n_rows']}，剔除违规 {res['n_excluded']}"
             f"，缺预测 {res['n_no_prediction']}）——{res['method']}"]
    for t, e in res["per_tag"].items():
        if e["n"] == 0:
            continue
        m = f"{e['mean_div']:+.4f}" if e["mean_div"] is not None else "—"
        mr = (f"{e['mean_div_rest']:+.4f}"
              if e["mean_div_rest"] is not None else "—")
        pv = f"{e['p_value']:.3f}" if e["p_value"] is not None else "n/a"
        lines.append(f"  {t:16s} n={e['n']:3d}（病例 {e['n_case']}/"
                     f"对照 {e['n_control']}）mean_div={m} vs 无标层 {mr}"
                     f"（n={e['n_rest']}）p={pv}")
    lines.append("  判读：标签层 mean_div 明显大于无标层且有样本量支撑 →"
                 "标签有信息量；各层无差 → 归因是叙事不是科学（如实报告）")
    return "\n".join(lines)
```

- [ ] **Step 4: CLI 命令**（src/fa/cli.py，retro 组内 `retro_consistency` 之后）

```python
@retro_app.command("analyze")
def retro_analyze(
    batch_id: int = typer.Option(None, "--batch-id",
                                 help="限定批；空=全库"),
    include_violations: bool = typer.Option(
        False, "--include-violations",
        help="含 audit 违规行（默认剔除——§15 裁定前不作分层依据）"),
) -> None:
    """关卡 3：按 miss_tags 分层的预测效度检验（点估计+Mann-Whitney U）"""
    from fa.retro.analyze import render_analysis_report, stratified_analysis
    conn = connect()
    try:
        res = stratified_analysis(conn, batch_id=batch_id,
                                  include_violations=include_violations)
    finally:
        conn.close()
    typer.echo(render_analysis_report(res))
```

- [ ] **Step 5: 设计档写回**（docs/superpowers/specs/2026-09-04-retro-attribution-design.md）

§8-3 末尾追加一句：「**方法（v1 实现，2026-09-04）**：显著性检验用 Mann-Whitney U（双侧，单元=单场 log-loss 差 `ln(mkt_p/model_p)`，任一侧 n<8 用 exact），代表行取聚合行优先/单成员行次之，audit 违规行默认剔除——`fa retro analyze` 已落地」。

§11 CLI 列表 `- fa retro analyze` 行去掉「（未实现）」类注记（若原文即无则只补 help 描述一致性）。§15 两处开放问题闭环：「显著性检验方法随 analyze 实现定并写回本档」处补「已定，见 §8-3」；「paper 注场次赛果结算口径 | Stage 2 前核对 M3 结算路径，赛果以 matches 表为准」处补「已核对（2026-09-04）：settle_paper_bets 配对即 `(league, home, away)` + kickoff±2 天窗取 matches 完赛行，paper_t1 选择器复用同模式（select._pair_match），赛果口径同源」。

- [ ] **Step 6: 跑全量 + Commit**

Run: `uv run pytest tests/retro/ tests/report/ -q`
Expected: 全 PASS（新增 5 条；audit 既有用例不破——重构行为不变）

```bash
git add src/fa/retro/analyze.py src/fa/cli.py tests/retro/test_cli_retro.py \
        docs/superpowers/specs/2026-09-04-retro-attribution-design.md
git commit -m "feat(retro): fa retro analyze 关卡3分层检验——MWU双侧+audit违规行默认剔除+方法写回设计档"
```

---

## Self-Review 记录

（写计划时的自检结果；实施者完成全部任务后在此追加实测记录）

1. **Spec 覆盖**：§4 paper_t1 行 → Task 1；§3/§10「日报摘要段落」→ Task 2+3；§10 Stage 2「挂 M3 paper 流次日」→ Task 3；§8-3 关卡 3 → Task 4；§8-1 audit 单一事实源重构 → Task 4（行为不变）；§15 两处开放问题（显著性方法、赛果结算口径核对）→ Task 4 Step 5 写回闭环。未覆盖：§8-2 一致性（已实现于 consistency_report）、Stage 3（不在本计划范围）。
2. **时间炸弹**：select_paper_t1 显式 target_date；daily._yesterday 独立函数可 monkeypatch；测试全部死日期（2024-04-20 / 2024-09-02 / 1999-01-01 钉既有用例前提）。
3. **方向性代入**：div 正=模型差（select.py 文档串原文）；Task 1 测试的 fixture 主客 id 对照 _seed 实际映射核对过（(1,3)=Arsenal vs West Ham → match 1，写 (1,2) 会错配到 match 3——已在种子注释里标死）；Task 4 手算口径（injury 层 +0.1446 vs 对照 −0.1395）先验算再断言。
4. **阈值可达性**：手算 div 用 test_select._seed 既有预验算值；MWU 任一侧 n<2→None 守卫与「p_value None」断言一致；主用例推送断言前置于 settled>0 种子（_seed_settleable）之后。
5. **mock 顺序**：全部 monkeypatch 使用方模块属性（daily.run_retro_batch / daily._yesterday），不 patch 定义方；与既有 test_daily 同法。
6. **单一事实源**：div 用 select._divergence 复用不自写；违规判定提取 _row_violation 供 audit/analyze 共用；_seed_yesterday_rec 的 id 全 lastrowid（与 _seed_settleable 共库不错位）。
7. **诚实条款预写**：stratified_analysis 文档串与 render_analysis_report 判读行均预写「不得调口径凑显著；各层无差=归因是叙事不是科学」。
8. **防呆段落**：无「先错后改」双版本代码（初稿的三处残渣已在自检中清除：标签分布 f-string/三元、SQL 草稿 if-else、条件弱化断言）。
