# M4 Persona 层 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Hermes persona 接入——5 个联赛人格、I/O 契约校验、场次级降级、A/B 双轨落库与分轨记账，使 §12.3 的 model_persona vs model_only 对比开始积累前向数据。

**Architecture:** 新包 `src/fa/persona/`（caller 子进程 / contract I-O / apply 判决落库）按 spec §9.2；value 层同刻双落两轨（model_persona 轨中性初始，persona 事后回填三列+点评两列）；paper 层 bankroll 按 strategy 分轨；matchday 管线 value → **persona** → bet 插层，pm 沿用 am 判决只补新增场次。mock 与实跑同一代码路径（`HERMES_BIN` 指向 fixture 脚本）。

**Tech Stack:** 既有栈零新增（stdlib subprocess/json/sqlite3 + typer/pytest）。

**Spec:** `spec.md` v0.5 §6（契约）、§7.2/7.3（双轨落注与指标）、§9.2/9.6/9.7（目录/调度/测试）、§10 M4、§12.3（预注册判据）；实现设计 `docs/superpowers/specs/2026-09-04-m4-persona-design.md`（决策 D1-D4 与 A1/B1/C1 的理由）。执行者必读 spec §6 与设计文档 §2。

## Global Constraints

- **契约权威在 spec §6.3**：输出校验词表/值域逐字——`verdict ∈ {agree, downweight, veto}`；`confidence_delta ∈ [−0.15, +0.15]`；downweight 时必须 < 0；`key_factors` 1–5 条每条 ≤ 50 字；`report_md` ≤ 500 字。**严格校验不做静默修复**；唯一例外 veto 时 delta 由 apply 强制置 0（判决语义非修号）
- **降级场次级**（D1）：超时 / hermes 非 0 exit / 剥不出合法 JSON / §6.3 校验失败 → **该场**三列保持中性（`verdict=NULL`、`final_stake_frac=kelly_stake_frac`）、点评两列 NULL；记 `runs.summary.persona`；当日不重试
- **表边界（§12.1）**：persona/管线不写 `matches`/`backtest_predictions`；persona 阶段只 UPDATE `recommendations`、写 `runs.summary`/`meta`
- **bankroll 分轨**（D2）：`paper_bankroll:model_only` / `paper_bankroll:model_persona` 各惰性初始化 1000；ROI/CLV/bankroll 全按轨统计
- **网络只许出现在** `fa/persona/caller.py`（hermes 子进程）与既有 `fa/pipeline/odds_api.py`/`fa/report/telegram.py`；单元测试一律离线（HERMES_BIN fixture 脚本 / monkeypatch）
- 串行逐场调用（B1）；超时默认 120s 可配（§6.2）；mock 与实跑同一 subprocess 代码路径（C1）
- 依赖零新增；提交 conventional；CLI 输出中文；测试命令一律 `uv run pytest ... -v`
- 每个公开函数恰好一次 `conn.commit()`（管线纪律）；时间经各模块 `_now` 注入缝
- **诚实条款**：实跑统计（不合规率/降级率/判决分布）只报实测数字，不得调参凑结论；A/B 两轨口径分开表述（§12.3 预注册判据）

---

### Task 1: spec 修订（先改设计再改代码）

**Files:**
- Modify: `spec.md`（§6.5 一处、§7.3 一处）

**Interfaces:**
- Produces: spec 与实现裁定一致（后续任务的合规依据）

- [ ] **Step 1: §6.5 降级粒度改场次级**

原文（§6.5 第一行）：

> 超时 / 解析失败 / JSON 不合规 / 超出值域 → **该联赛**自动回退纯模型推荐，报告中标注「persona 未生效 + 原因」。降级事件记入 `runs.summary`。

改为：

> 超时 / 解析失败 / JSON 不合规 / 超出值域 → **该场次**自动回退纯模型推荐，报告中标注「persona 未生效 + 原因」。降级事件记入 `runs.summary`。（粒度裁定 2026-09-04：调用本按场次发起，一场失败不撤销同联赛已成功判决；当日降级不重试。）

- [ ] **Step 2: §7.3 表格后补分轨口径**

在 §7.3 的指标表之后新增一行：

> paper bankroll 与上表指标按 `strategy` 分轨统计（model_only / model_persona 各一本，初始各 1000）——§12.3 预注册判据的分轨对比口径（M4 裁定，2026-09-04：分轨隔离使一轨盈余不放大另一轨仓位，A/B 对比不失真）。

- [ ] **Step 3: 验证 + Commit**

Run: `grep -n "该场次" spec.md && grep -n "分轨统计" spec.md`
Expected: 各 1 命中

```bash
git add spec.md && git commit -m "docs(spec): §6.5 降级粒度改场次级、§7.3 补 bankroll 分轨口径（M4 设计裁定）"
```

---

### Task 2: 探针——hermes -z 工具集控制实测

**Files:**
- Create: `docs/m4-report.md`（骨架 + 探针节）
- Modify: 无代码（产出决定 Task 5 的 `build_command`）

**Interfaces:**
- Produces: `docs/m4-report.md` §探针 的结论行（Task 5 据此落定命令形态：是否带 `-t` / 额外参数）

- [ ] **Step 1: 建报告骨架**（`docs/m4-report.md`）：

```markdown
# M4 验收报告（persona 接入）

- 日期：2026-09-04 起
- 代码：branch `worktree-m4-persona-design`
- 本报告所有数字均为实跑测得，无推算值

## 0. 探针：hermes -z 工具集控制（§6.1 白名单落地）

（结论待填：-t 参数形态 / 工具授权行为 / build_command 最终形态）
```

- [ ] **Step 2: 实测**（三条命令逐条跑，记录 exit 与输出尾部）：

```bash
hermes -z -t web_search '只输出一个 JSON 对象 {"probe": "websearch-only"}' ; echo "exit=$?"
hermes -z '只输出一个 JSON 对象 {"probe": "default-tools"}}' ; echo "exit=$?"
hermes tools list 2>&1 | head -20    # 或 hermes --help 的 toolsets 说明
```

判定：`-t web_search` 能收窄工具集 → `build_command` 带 `-t web_search`；无法限制 → 不带 `-t` 且不启用 `--yolo`（设计文档 §8 回退语义），白名单落地程度如实记入本节。

- [ ] **Step 3: 结论写进 m4-report §0 并 Commit**

```bash
git add docs/m4-report.md && git commit -m "docs: m4-report 骨架与探针结论（hermes -z 工具集控制）"
```

---

### Task 3: config 追加——hermes/persona 配置

**Files:**
- Modify: `src/fa/config.py`（文件尾部追加）
- Test: `tests/test_config.py`（追加）

**Interfaces:**
- Produces:
  - `PERSONA_TIMEOUT_S = 120.0`（spec §6.2 默认）
  - `PERSONA_FILES: dict[str, str]`（E0→epl.md、SP1→laliga.md、D1→bundesliga.md、I1→seriea.md、F1→ligue1.md，spec §9.2）
  - `hermes_bin() -> str`（env `HERMES_BIN`，默认 `"hermes"`）
  - `persona_timeout() -> float`（env `FA_PERSONA_TIMEOUT`，默认 `PERSONA_TIMEOUT_S`；非法数值回退默认）
  - `persona_path(league: str) -> Path`（`project_root()/"personas"/PERSONA_FILES[league]`；未知 league 上抛 `ValueError`）

- [ ] **Step 1: 失败测试**（追加到 `tests/test_config.py`）

```python
from pathlib import Path

from fa.config import (PERSONA_FILES, PERSONA_TIMEOUT_S, hermes_bin,
                       persona_path, persona_timeout)


def test_persona_files_cover_five_leagues():
    assert set(PERSONA_FILES) == {"E0", "SP1", "D1", "I1", "F1"}


def test_persona_path_maps_and_rejects(tmp_path, monkeypatch):
    monkeypatch.setattr("fa.config.project_root", lambda: tmp_path)
    assert (persona_path("D1") ==
            tmp_path / "personas" / "bundesliga.md")
    with pytest.raises(ValueError):
        persona_path("XX")


def test_hermes_bin_env_override(monkeypatch):
    monkeypatch.setenv("HERMES_BIN", "/tmp/fake-hermes")
    assert hermes_bin() == "/tmp/fake-hermes"
    monkeypatch.delenv("HERMES_BIN")
    assert hermes_bin() == "hermes"


def test_persona_timeout_default_and_override(monkeypatch):
    monkeypatch.delenv("FA_PERSONA_TIMEOUT", raising=False)
    assert persona_timeout() == PERSONA_TIMEOUT_S == 120.0
    monkeypatch.setenv("FA_PERSONA_TIMEOUT", "5")
    assert persona_timeout() == 5.0
    monkeypatch.setenv("FA_PERSONA_TIMEOUT", "not-a-number")
    assert persona_timeout() == 120.0        # 非法值回退默认，不炸
```

- [ ] **Step 2: 跑测确认失败**

Run: `uv run pytest tests/test_config.py -v -k "persona or hermes"`
Expected: FAIL（`ImportError: cannot import name 'PERSONA_FILES'`）

- [ ] **Step 3: 实现**（`src/fa/config.py` 尾部追加）

```python
# ---------------------------------------------------------------- persona（M4，spec §6）

PERSONA_TIMEOUT_S = 120.0            # §6.2「超时默认 120s（可配）」
PERSONA_FILES = {                    # §9.2 目录规划（personas/ 文件名）
    "E0": "epl.md", "SP1": "laliga.md", "D1": "bundesliga.md",
    "I1": "seriea.md", "F1": "ligue1.md",
}


def hermes_bin() -> str:
    """hermes 可执行文件路径；测试以 env HERMES_BIN 指向 fixture 脚本（C1：mock
    与实跑同一 subprocess 代码路径）。"""
    return os.environ.get("HERMES_BIN") or "hermes"


def persona_timeout() -> float:
    """persona 单次调用超时秒数（env FA_PERSONA_TIMEOUT 覆盖；非法值回退默认）。"""
    raw = os.environ.get("FA_PERSONA_TIMEOUT")
    if raw is None:
        return PERSONA_TIMEOUT_S
    try:
        return float(raw)
    except ValueError:
        return PERSONA_TIMEOUT_S


def persona_path(league: str) -> Path:
    """联赛 → personas/ 下的人格文件路径（spec §9.2）。"""
    try:
        name = PERSONA_FILES[league]
    except KeyError:
        raise ValueError(f"无 persona 文件映射：{league!r}") from None
    return project_root() / "personas" / name
```

- [ ] **Step 4: 跑测确认通过**

Run: `uv run pytest tests/test_config.py -v`
Expected: PASS（全文件，含既有用例）

- [ ] **Step 5: Commit**

```bash
git add src/fa/config.py tests/test_config.py
git commit -m "feat(config): hermes/persona 配置——HERMES_BIN、超时可配、persona 文件映射"
```

---

### Task 4: schema v4——recommendations 补 persona 点评两列

spec §6.3 输出的 `key_factors`/`report_md` 无处落库（M3 只建了 verdict/delta/final 三列），加法迁移补两列。**DO UPDATE 不触碰**（同三列语义：pm 刷新价格不清判决）。

**Files:**
- Modify: `src/fa/db.py`（SCHEMA_VERSION=4、`recommendations` DDL 加两列、`_migrate_up` 加 v3→v4）
- Test: `tests/test_db.py`（追加）

**Interfaces:**
- Produces: `recommendations` 表新增 `key_factors TEXT`（JSON 数组串）、`report_md TEXT`，均可空；既有列与数据不变

- [ ] **Step 1: 失败测试**（追加；四件套模式照抄 test_db 既有迁移用例——fresh=v4 / v3 升级保数据 / future 拒绝 / migrate 与 fresh 形状一致）

```python
def test_schema_v4_adds_persona_columns_fresh(tmp_path):
    init_db(tmp_path / "t.db")
    c = connect(tmp_path / "t.db")
    cols = {r["name"] for r in c.execute("PRAGMA table_info(recommendations)")}
    assert {"key_factors", "report_md"} <= cols
    ver = c.execute("SELECT version FROM schema_version").fetchone()["version"]
    assert ver == 4
    c.close()


def test_schema_v4_upgrade_preserves_v3_data(tmp_path):
    # 先以 v3 建库并落一行 recommendation（模拟 M3 库），再升级断言数据与新列共存
    init_db(tmp_path / "t.db")
    c = connect(tmp_path / "t.db")
    c.execute("UPDATE schema_version SET version=3")
    c.execute("ALTER TABLE recommendations DROP COLUMN key_factors")
    c.execute("ALTER TABLE recommendations DROP COLUMN report_md")
    c.execute("INSERT INTO recommendations (run_id, fixture_id, strategy, market,"
              " phase, model_p, market_p, best_odds, bookmaker, edge, ev,"
              " kelly_stake_frac, created_at)"
              " VALUES (1, 1, 'model_only', 'H', 'am', .5, .4, 2.0, 'b',"
              " .1, .2, .01, '2026-09-04T00:00:00Z')")
    c.commit()
    init_db(tmp_path / "t.db")                      # 再跑 init = 升级路径
    row = c.execute("SELECT strategy, key_factors, report_md FROM recommendations"
                    " WHERE id=1").fetchone()
    assert row["strategy"] == "model_only"
    assert row["key_factors"] is None and row["report_md"] is None
    c.close()
```

（若既有 test_db 已有「fresh 与 migrate 形状一致」的参数化用例，把它扩到 v4 断言即可，不另起新函数。）

- [ ] **Step 2: 跑测确认失败**

Run: `uv run pytest tests/test_db.py -v -k v4`
Expected: FAIL（无 v4 / 新列不存在）

- [ ] **Step 3: 实现**

- `SCHEMA_VERSION = 4`
- `_BLINE_TABLE` 中 `recommendations` 的 DDL：在 `final_stake_frac REAL,` 行后加：

```sql
    key_factors      TEXT,                   -- M4 persona：JSON 数组串（§6.3，1–5 条）
    report_md        TEXT,                   -- M4 persona：点评 ≤500 字（§6.3）
```

- `_migrate_up` 加分支（既有 v2→v3 同款 try/except 形态）：

```python
if from_version <= 3:
    conn.execute("ALTER TABLE recommendations ADD COLUMN key_factors TEXT")
    conn.execute("ALTER TABLE recommendations ADD COLUMN report_md TEXT")
```

- [ ] **Step 4: 跑测确认通过 + 全量回归**

Run: `uv run pytest tests/test_db.py -v && uv run pytest -q`
Expected: PASS（400+，v3 真库升级由执行者在 worktree 库上另跑一次 `uv run fa init` 验证）

- [ ] **Step 5: Commit**

```bash
git add src/fa/db.py tests/test_db.py
git commit -m "feat(db): schema v4——recommendations 补 persona 点评两列（key_factors/report_md）"
```

---

### Task 5: persona/caller.py——hermes -z 子进程

**Files:**
- Create: `src/fa/persona/__init__.py`、`src/fa/persona/caller.py`
- Test: `tests/persona/__init__.py`（空）、`tests/persona/test_caller.py`、`tests/persona/fixtures/hermes_{ok,bad_json,timeout,nonzero}`（可执行）

**Interfaces:**
- Consumes: `fa.config.hermes_bin / persona_timeout`（Task 3）
- Produces:
  - `fa.persona.PersonaError(RuntimeError)`（包级基类，apply 统一捕获）
  - `HermesCallError(PersonaError)`（`.reason` ∈ `{"timeout", "exit"}`）
  - `build_command(prompt: str) -> list[str]`（纯函数；命令形态以 Task 2 探针结论为准——下示默认形态，探针若支持则 `-z` 前插 `"-t", "web_search"`）
  - `call_hermes(prompt: str, timeout: float | None = None) -> str`（成功返回 stdout；超时/非 0 exit 抛 `HermesCallError`；**本函数是 persona 唯一触网点**）

- [ ] **Step 1: fixture 脚本**（`tests/persona/fixtures/` 下四个可执行文件，`chmod +x`）

```sh
#!/bin/sh
# hermes_ok——合法 JSON 输出
echo '{"verdict":"agree","confidence_delta":-0.05,"key_factors":["主队中场停赛"],"report_md":"### 点评\n主队轮换风险。"}'
```

```sh
#!/bin/sh
# hermes_bad_json——围栏+噪声，无合法 JSON
echo '让我想想…… persona 认为本场不适合 JSON。'
```

```sh
#!/bin/sh
# hermes_timeout——挂死等超时
sleep 30
```

```sh
#!/bin/sh
# hermes_nonzero——非 0 退出
echo "boom: no credits" >&2
exit 3
```

- [ ] **Step 2: 失败测试**（`tests/persona/test_caller.py`）

```python
"""caller 测试（C1）：HERMES_BIN 指向 fixture 脚本——真子进程、真超时、真 exit code。"""
import pytest

from fa.config import hermes_bin
from fa.persona.caller import HermesCallError, build_command, call_hermes

FIX = pytest.fixture(lambda: Path(__file__).parent / "fixtures")


def test_build_command_shape():
    cmd = build_command("你好")
    assert cmd[0] == hermes_bin()          # hermes_bin 可注入（env）
    assert "-z" in cmd and cmd[-1] == "你好"


def test_call_ok(monkeypatch, fix):
    monkeypatch.setenv("HERMES_BIN", str(fix / "hermes_ok"))
    out = call_hermes("prompt")
    assert '"verdict"' in out


def test_call_timeout(monkeypatch, fix):
    monkeypatch.setenv("HERMES_BIN", str(fix / "hermes_timeout"))
    monkeypatch.setenv("FA_PERSONA_TIMEOUT", "0.5")
    with pytest.raises(HermesCallError) as ei:
        call_hermes("prompt")
    assert ei.value.reason == "timeout"


def test_call_nonzero_exit(monkeypatch, fix):
    monkeypatch.setenv("HERMES_BIN", str(fix / "hermes_nonzero"))
    with pytest.raises(HermesCallError) as ei:
        call_hermes("prompt")
    assert ei.value.reason == "exit"
    assert "3" in str(ei.value)            # 退出码可溯源
```

（`pytest.fixture(lambda: ...)` 若语法受限，就写成普通 `@pytest.fixture` 函数 `def fix(): return ...`。）

- [ ] **Step 3: 跑测确认失败**

Run: `uv run pytest tests/persona/test_caller.py -v`
Expected: FAIL（模块不存在）

- [ ] **Step 4: 实现**

`src/fa/persona/__init__.py`：

```python
"""persona 层（spec §6）：hermes 调用、契约校验、判决应用。agent 只消费/产出
结构化 JSON，不碰库、不发明数字（§1.4/§2.2）。"""


class PersonaError(RuntimeError):
    """persona 层基类：所有可降级失败（超时/exit/提取/校验/文件缺失）。"""
```

`src/fa/persona/caller.py`：

```python
"""hermes -z headless 子进程调用（spec §6.2）——persona 唯一触网点。

C1（设计文档 §2）：mock 与实跑同一代码路径——测试把 ``HERMES_BIN`` 指向
fixture 脚本，超时 / exit code / stdout 噪声全部真实。
命令形态由 Task 2 探针结论钉死（§6.1 白名单仅 web_search 的落地方式）。
"""
from __future__ import annotations

import subprocess

from fa.config import hermes_bin, persona_timeout
from fa.persona import PersonaError


class HermesCallError(PersonaError):
    """子进程失败：``reason`` 为 "timeout"（超时）或 "exit"（非 0 退出）。"""

    def __init__(self, reason: str, detail: str = ""):
        super().__init__(f"{reason}: {detail}" if detail else reason)
        self.reason = reason


# 探针结论落定后如需 -t web_search，在此列表补齐（build_command 唯一事实源）
_EXTRA_ARGS: list[str] = []


def build_command(prompt: str) -> list[str]:
    return [hermes_bin(), *_EXTRA_ARGS, "-z", prompt]


def call_hermes(prompt: str, timeout: float | None = None) -> str:
    """调一次 hermes，返回 stdout；超时 / 非 0 exit → :class:`HermesCallError`。"""
    try:
        proc = subprocess.run(build_command(prompt), capture_output=True,
                              text=True, timeout=timeout or persona_timeout())
    except subprocess.TimeoutExpired as exc:
        raise HermesCallError("timeout", f"{exc.timeout}s") from exc
    if proc.returncode != 0:
        tail = (proc.stderr or proc.stdout or "").strip().splitlines()[-1:]
        raise HermesCallError("exit", f"code {proc.returncode} {tail}")
    return proc.stdout
```

- [ ] **Step 5: 跑测确认通过**

Run: `uv run pytest tests/persona/test_caller.py -v`
Expected: 4 PASS

- [ ] **Step 6: Commit**

```bash
git add src/fa/persona/ tests/persona/
git commit -m "feat(persona): hermes -z caller——HERMES_BIN 可配、超时/exit 分类、mock 同路径"
```

---

### Task 6: contract 输入侧——build_input / build_prompt

**Files:**
- Create: `src/fa/persona/contract.py`
- Test: `tests/persona/test_contract_input.py`

**Interfaces:**
- Consumes: `recommendations`（model_persona 轨行）、`fixtures`/`teams`/`matches`（只读）
- Produces:
  - `build_input(conn, fixture_id: int) -> dict`——§6.3 输入 JSON 的 dict 形态：`league` / `match{kickoff_utc, home, away}` / `candidates[{market, model_p, market_p, best_odds, edge, ev, kelly_stake_frac}]`（该场 model_persona 轨全部行）/ `model_summary{p_home?, p_draw?, p_away?}`（从 H/D/A 行的 `model_p` 拼装；**λ 不在库内，不含**——对 spec 示例的有意裁剪）/ `form{home_last5, away_last5}`（W/D/L 串，matches 该队 kickoff 前近 5 场，跨赛季照取）/ `h2h_recent[{date, score, home}]`（近 3 次交手）/ `home_pos`/`away_pos`（本赛季截至 kickoff 的积分榜排名；赛季无数据则两键俱缺）
  - `build_prompt(persona_md: str, input_obj: dict) -> str`（persona 全文 + 输入 JSON + 契约说明，§6.2）

- [ ] **Step 1: 失败测试**（种子直插，模式同 tests/pipeline/test_paper.py）

```python
"""contract 输入侧测试：种子库 → build_input 形状 + form/h2h/排名的查询语义。"""
from fa.db import connect, init_db
from fa.persona.contract import build_input, build_prompt

NOW = "2026-09-10"          # kickoff 当日；form/h2h 严格早于该日


# ---------------- 种子（队/赛果/fixture/推荐 两行）——写成模块级辅助，见 test_paper 风格
# home 队近 5 场 W W D L W；away 队近 5 场 L L D W L；两队上赛季交手 2 次
# fixtures 一行（kickoff=2026-09-10T18:45Z 双侧已对齐）+ recommendations
# model_persona 轨两行（H 与 O2.5）


def test_build_input_shape(conn_seeded):
    obj = build_input(conn_seeded, FIXTURE_ID)
    assert obj["league"] == "D1"
    assert obj["match"]["home"] == "Bayern Munich"
    assert [c["market"] for c in obj["candidates"]] == ["H", "O2.5"]
    assert obj["model_summary"]["p_home"] == pytest.approx(0.52, abs=1e-9)
    assert "exp_goals" not in str(obj)            # λ 不在库内，不发明
    assert obj["form"]["home_last5"] == ["W", "W", "D", "L", "W"]
    assert obj["h2h_recent"][0]["score"] == "4-2"


def test_build_input_omits_empty_sections(conn_seeded):
    obj = build_input(conn_seeded, FIXTURE_ID_NO_HISTORY)
    assert obj["h2h_recent"] == []
    assert "home_pos" not in obj                   # 无本赛季数据 → 两键俱缺


def test_build_prompt_layout():
    prompt = build_prompt("PERSONA 全文", {"league": "D1"})
    assert prompt.startswith("PERSONA 全文")
    assert '"league": "D1"' in prompt
    assert "verdict" in prompt and "confidence_delta" in prompt   # 契约说明在场
```

（`conn_seeded` 夹具与 `FIXTURE_ID` 常量由执行者按注释种子语义补全——队名/日期换算：近 5 场按 `date DESC` 取，队视角 W/D/L = fthg/ftag 与 home_team_id 比对。）

- [ ] **Step 2: 跑测确认失败** → FAIL（模块不存在）

Run: `uv run pytest tests/persona/test_contract_input.py -v`

- [ ] **Step 3: 实现**（`src/fa/persona/contract.py` 输入节）

```python
"""persona I/O 契约（spec §6.3）：输入组装（全来自库内已有数据）与输出提取校验。

输入裁剪（对 §6.3 示例的有意裁定）：``model_summary`` 只含 H/D/A 行的 ``model_p``
（λ 不在库内，不发明数字）；``home_pos``/``away_pos`` 赛季无数据则缺省。
时间口径：form/h2h 取 kickoff **日历日之前**的完赛行（当日同场不是历史）。
"""
from __future__ import annotations

import json
import sqlite3

from fa.persona import PersonaError

_MARKET_TO_KEY = {"H": "p_home", "D": "p_draw", "A": "p_away"}

_CONTRACT_CLAUSE = """

## 输出契约（必须严格遵守）
输出必须是且仅是一个 JSON 对象（不要代码围栏、不要任何多余文字），字段：
- verdict：三选一 "agree" / "downweight" / "veto"
- confidence_delta：[-0.15, +0.15] 内的数；downweight 时必须 < 0；veto 时填 0
- key_factors：1–5 条字符串数组，每条 ≤ 50 字
- report_md：≤ 500 字中文点评（markdown）
不合规的输出会被程序整场丢弃（该场按纯模型处理），宁保守勿越界。
"""


def build_prompt(persona_md: str, input_obj: dict) -> str:
    return (persona_md.rstrip() + "\n\n## 本场输入\n```json\n"
            + json.dumps(input_obj, ensure_ascii=False, indent=2)
            + "\n```" + _CONTRACT_CLAUSE)
```

`build_input` 主体（查询按 Interfaces 描述落 SQL；form 查询示例——away 队同理）：

```python
def _last5(conn, league, team_id, before_date):
    rows = conn.execute(
        "SELECT date, fthg, ftag, home_team_id FROM matches"
        " WHERE league=? AND (home_team_id=? OR away_team_id=?) AND date<?"
        " ORDER BY date DESC, id DESC LIMIT 5",
        (league, team_id, team_id, before_date)).fetchall()
    out = []
    for r in rows:
        gf, ga = (r["fthg"], r["ftag"]) if r["home_team_id"] == team_id \
            else (r["ftag"], r["fthg"])
        out.append("W" if gf > ga else "D" if gf == ga else "L")
    return out
```

排名：`SELECT home_team_id, away_team_id, fthg, ftag FROM matches WHERE league=? AND season=? AND date<?` 聚合积分（胜 3 平 1），返回 `{team_id: (积分, 净胜球, 队名)}` 排名；赛季推断 `season = kickoff.year if kickoff.month >= 7 else kickoff.year - 1`；无行 → 键缺省。

- [ ] **Step 4: 跑测确认通过**

Run: `uv run pytest tests/persona/test_contract_input.py -v`
Expected: 3 PASS

- [ ] **Step 5: Commit**

```bash
git add src/fa/persona/contract.py tests/persona/test_contract_input.py
git commit -m "feat(persona): 输入组装——candidates/form/h2h/排名全自库内 + prompt 拼装"
```

---

### Task 7: contract 输出侧——extract_json / validate_output

**Files:**
- Modify: `src/fa/persona/contract.py`（追加输出节）
- Test: `tests/persona/test_contract_output.py`

**Interfaces:**
- Produces:
  - `PersonaContractError(PersonaError)`（`.reason` ∈ `{"extract", "contract"}`）
  - `extract_json(text: str) -> dict`——先整体 `json.loads`；失败剥 ```json 围栏；再失败做 `{` 起始的平衡花括号扫描（处理字符串内花括号）；全败 → `PersonaContractError("extract")`
  - `validate_output(obj: dict) -> None`——§6.3 逐条；违规 → `PersonaContractError("contract", 具体原因)`。**veto 的 delta 不在此卡**（apply 强制置 0，设计文档 §5）

- [ ] **Step 1: 失败测试**

```python
"""contract 输出侧测试：提取矩阵 + 校验矩阵（合法与非法边界逐条）。"""
import pytest

from fa.persona.contract import (PersonaContractError, extract_json,
                                 validate_output)

GOOD = {"verdict": "downweight", "confidence_delta": -0.05,
        "key_factors": ["a"], "report_md": "x"}


def test_extract_plain():
    assert extract_json('{"verdict":"agree"}') == {"verdict": "agree"}


def test_extract_fenced():
    text = '点评如下：\n```json\n{"verdict": "agree"}\n```\n以上。'
    assert extract_json(text) == {"verdict": "agree"}


def test_extract_noise_with_embedded_json():
    text = '思考……{"verdict": "agree", "report_md": "含 } 花括号"} 结语'
    assert extract_json(text)["verdict"] == "agree"


def test_extract_failure():
    with pytest.raises(PersonaContractError) as ei:
        extract_json("persona 拒绝输出 JSON")
    assert ei.value.reason == "extract"


@pytest.mark.parametrize("obj, why", [
    ({**GOOD, "verdict": "maybe"}, "词表外"),
    ({**GOOD, "confidence_delta": 0.2}, "超上界"),
    ({**GOOD, "confidence_delta": -0.2}, "超下界"),
    ({**GOOD, "confidence_delta": 0.05}, "downweight 须 <0"),
    ({**GOOD, "key_factors": []}, "空数组"),
    ({**GOOD, "key_factors": ["a"] * 6}, "超 5 条"),
    ({**GOOD, "key_factors": ["字" * 51]}, "单条超 50 字"),
    ({**GOOD, "report_md": "字" * 501}, "超 500 字"),
    ({**GOOD, "confidence_delta": "0.05"}, "非数值"),
])
def test_validate_rejects(obj, why):
    with pytest.raises(PersonaContractError):
        validate_output(obj)


@pytest.mark.parametrize("obj", [
    {"verdict": "agree", "confidence_delta": 0.0, "key_factors": ["a"],
     "report_md": "x"},
    {"verdict": "veto", "confidence_delta": 0.1, "key_factors": ["a"],
     "report_md": "x"},                       # veto 的 delta 由 apply 置 0，校验放行
    {**GOOD, "key_factors": ["字" * 50], "report_md": "字" * 500},  # 边界值恰好合法
])
def test_validate_accepts(obj):
    assert validate_output(obj) is None
```

- [ ] **Step 2: 跑测确认失败** → FAIL（名字未定义）

Run: `uv run pytest tests/persona/test_contract_output.py -v`

- [ ] **Step 3: 实现**（contract.py 追加）

```python
class PersonaContractError(PersonaError):
    def __init__(self, reason: str, detail: str = ""):
        super().__init__(f"{reason}: {detail}" if detail else reason)
        self.reason = reason


def extract_json(text: str) -> dict:
    """stdout → JSON 对象：整体 → 围栏 → 平衡花括号扫描（字符串内 } 不误判）。"""
    stripped = (text or "").strip()
    for candidate in (stripped, _fenced(stripped)):
        if candidate is not None:
            try:
                obj = json.loads(candidate)
            except ValueError:
                continue
            if isinstance(obj, dict):
                return obj
    obj = _balanced_scan(stripped)
    if obj is not None:
        return obj
    raise PersonaContractError(
        "extract", f"stdout 无合法 JSON（前 80 字：{stripped[:80]!r}）")


def _fenced(text: str) -> str | None:
    if "```" not in text:
        return None
    parts = text.split("```")
    for i in range(1, len(parts) - 1, 2):        # 围栏内容在奇数段
        body = parts[i]
        body = body[4:] if body.lstrip().startswith("json") else body
        if "{" in body:
            return body.strip()
    return None


def _balanced_scan(text: str) -> dict | None:
    start = text.find("{")
    while start != -1:
        depth, in_str, escape = 0, False, False
        for i in range(start, len(text)):
            ch = text[i]
            if in_str:
                if escape:
                    escape = False
                elif ch == "\\":
                    escape = True
                elif ch == '"':
                    in_str = False
            elif ch == '"':
                in_str = True
            elif ch == "{":
                depth += 1
            elif ch == "}":
                depth -= 1
                if depth == 0:
                    try:
                        obj = json.loads(text[start:i + 1])
                    except ValueError:
                        break                      # 该起点不成 JSON，试下一个 {
                    return obj if isinstance(obj, dict) else None
        start = text.find("{", start + 1)
    return None


def validate_output(obj: dict) -> None:
    """§6.3 逐条；veto 的 delta 不卡（apply 置 0，设计文档 §5 裁定）。"""
    if obj.get("verdict") not in ("agree", "downweight", "veto"):
        raise PersonaContractError("contract", f"verdict 词表外：{obj.get('verdict')!r}")
    delta = obj.get("confidence_delta")
    if not isinstance(delta, (int, float)) or isinstance(delta, bool):
        raise PersonaContractError("contract", f"confidence_delta 非数值：{delta!r}")
    if not -0.15 <= delta <= 0.15:
        raise PersonaContractError("contract", f"confidence_delta 超值域：{delta}")
    if obj["verdict"] == "downweight" and delta >= 0:
        raise PersonaContractError("contract", f"downweight 须 <0：{delta}")
    factors = obj.get("key_factors")
    if (not isinstance(factors, list) or not 1 <= len(factors) <= 5
            or not all(isinstance(f, str) for f in factors)):
        raise PersonaContractError("contract", f"key_factors 须 1–5 条字符串：{factors!r}")
    if any(len(f) > 50 for f in factors):
        raise PersonaContractError("contract", "key_factors 单条超 50 字")
    report = obj.get("report_md")
    if not isinstance(report, str) or len(report) > 500:
        raise PersonaContractError("contract", f"report_md 须 ≤500 字：{report!r}")
```

- [ ] **Step 4: 跑测确认通过**

Run: `uv run pytest tests/persona/test_contract_output.py -v`
Expected: 全 PASS

- [ ] **Step 5: Commit**

```bash
git add src/fa/persona/contract.py tests/persona/test_contract_output.py
git commit -m "feat(persona): 输出提取（围栏/噪声/平衡扫描）与 §6.3 严格校验"
```

---

### Task 8: persona/apply.py——判决落库与阶段编排

**Files:**
- Create: `src/fa/persona/apply.py`
- Test: `tests/persona/test_apply.py`

**Interfaces:**
- Consumes: Task 5 caller、Task 6/7 contract、Task 3 persona_path、Task 4 两新列
- Produces:
  - `apply_verdict(conn, fixture_id: int, output: dict) -> int`——§6.4 映射广播到该 fixture 的 **model_persona 轨全部行**（不分 phase：am 判决 pm 行共用，§6.2「pm 沿用」）；agree/downweight → `final_stake_frac = kelly × (1+delta)` 逐行按各自 kelly；veto → `final_stake_frac=0`、`confidence_delta=0`；同写 `verdict/key_factors(JSON 串)/report_md`；返回更新行数
  - `run_persona_phase(conn, run_id: int, leagues: list[str], attempted: set[int] | None = None) -> dict`——阶段编排：选该 run 的 model_persona 候选 fixture（去重、`attempted` 里的跳过——pm 沿用 am 判决），逐场 persona 文件→build_input→build_prompt→call_hermes→extract→validate→apply_verdict；`PersonaError`/`FileNotFoundError` → 该场降级（reason：`persona_file`/`timeout`/`exit`/`extract`/`contract`）。返回 `{"called": int, "ok": int, "veto": int, "degraded": [{"fixture_id", "reason"}], "attempted": sorted list}`——`attempted` = 传入 ∪ 本次涉及（am 的落 runs.summary 供 pm 读）。恰好一次 `conn.commit()`

- [ ] **Step 1: 失败测试**

```python
"""apply 测试：§6.4 映射 / 场次广播 / veto 语义 / 阶段编排（fixture 脚本走真子进程）。"""
import pytest

from fa.persona.apply import apply_verdict, run_persona_phase

# 种子：一 fixture 的 model_persona 轨两行（H: kelly=0.02, O2.5: kelly=0.01）+
#       一 run 行（run_persona_phase 需要 run_id 归属）


def test_apply_agree_broadcasts_with_per_row_kelly(conn_seeded):
    n = apply_verdict(conn_seeded, FID,
                      {"verdict": "agree", "confidence_delta": 0.1,
                       "key_factors": ["a"], "report_md": "x"})
    assert n == 2
    rows = conn_seeded.execute(
        "SELECT market, final_stake_frac, confidence_delta FROM recommendations"
        " WHERE fixture_id=? AND strategy='model_persona' ORDER BY market",
        (FID,)).fetchall()
    assert rows[0]["final_stake_frac"] == pytest.approx(0.02 * 1.1, abs=1e-9)
    assert rows[1]["final_stake_frac"] == pytest.approx(0.01 * 1.1, abs=1e-9)


def test_apply_veto_zeroes_all(conn_seeded):
    apply_verdict(conn_seeded, FID,
                  {"verdict": "veto", "confidence_delta": 0.12,   # 强制置 0
                   "key_factors": ["a"], "report_md": "x"})
    rows = conn_seeded.execute(
        "SELECT final_stake_frac, confidence_delta, verdict, key_factors,"
        " report_md FROM recommendations WHERE fixture_id=?"
        " AND strategy='model_persona'", (FID,)).fetchall()
    for r in rows:
        assert r["final_stake_frac"] == 0.0
        assert r["confidence_delta"] == 0.0
        assert r["verdict"] == "veto"
        assert r["key_factors"] == '["a"]'        # JSON 串落库


def test_phase_ok_degraded_and_attempted(conn_seeded, monkeypatch, fix):
    monkeypatch.setenv("HERMES_BIN", str(fix / "hermes_ok"))
    out = run_persona_phase(conn_seeded, RUN_ID, ["D1"])
    assert out["called"] == out["ok"] == 1 and out["attempted"] == [FID]


def test_phase_degrades_on_bad_json_and_remember_attempted(
        conn_seeded, monkeypatch, fix):
    monkeypatch.setenv("HERMES_BIN", str(fix / "hermes_bad_json"))
    out = run_persona_phase(conn_seeded, RUN_ID, ["D1"])
    assert out["ok"] == 0 and out["degraded"][0]["reason"] == "extract"
    assert out["attempted"] == [FID]              # 降级也算已尝试（pm 不重试）
    row = conn_seeded.execute(
        "SELECT verdict FROM recommendations WHERE fixture_id=?"
        " AND strategy='model_persona'", (FID,)).fetchone()
    assert row["verdict"] is None                 # 中性保持


def test_phase_skips_attempted(conn_seeded, monkeypatch, fix):
    monkeypatch.setenv("HERMES_BIN", str(fix / "hermes_ok"))
    out = run_persona_phase(conn_seeded, RUN_ID, ["D1"], attempted={FID})
    assert out["called"] == 0                     # pm 沿用 am 判决，零调用
```

（`fix` 夹具同 Task 5；`conn_seeded` 种子语义写在文件 docstring：两行 recs 挂 `RUN_ID` 的 run。）

- [ ] **Step 2: 跑测确认失败** → FAIL（模块不存在）

- [ ] **Step 3: 实现**（`src/fa/persona/apply.py`）

```python
"""persona 判决应用与阶段编排（spec §6.4 / §6.5 / §6.2）。

降级只回退该场（D1）：三列保持中性、点评两列不动、记入返回值 degraded——
调用方（matchday）把它写进 runs.summary.persona；当日不重试（attempted 语义）。
"""
from __future__ import annotations

import json
import sqlite3

from fa.config import persona_path
from fa.persona import PersonaError
from fa.persona.caller import call_hermes
from fa.persona.contract import build_input, build_prompt, extract_json, validate_output

STRATEGY = "model_persona"

_REASON = {"timeout": "timeout", "exit": "exit",
           "extract": "extract", "contract": "contract"}


def apply_verdict(conn: sqlite3.Connection, fixture_id: int,
                  output: dict) -> int:
    verdict = output["verdict"]
    delta = 0.0 if verdict == "veto" else float(output["confidence_delta"])
    factors = json.dumps(output["key_factors"], ensure_ascii=False)
    updated = 0
    for row in conn.execute(
            "SELECT id, kelly_stake_frac FROM recommendations"
            " WHERE fixture_id=? AND strategy=?",
            (fixture_id, STRATEGY)).fetchall():
        final = 0.0 if verdict == "veto" else row["kelly_stake_frac"] * (1 + delta)
        conn.execute(
            "UPDATE recommendations SET verdict=?, confidence_delta=?,"
            " final_stake_frac=?, key_factors=?, report_md=? WHERE id=?",
            (verdict, delta, round(final, 6), factors,
             output["report_md"], row["id"]))
        updated += 1
    return updated


def run_persona_phase(conn: sqlite3.Connection, run_id: int,
                      leagues: list[str], attempted: set[int] | None = None) -> dict:
    seen = set(attempted or ())
    called = ok = veto = 0
    degraded: list[dict] = []
    rows = conn.execute(
        "SELECT DISTINCT r.fixture_id, f.league FROM recommendations r"
        " JOIN fixtures f ON f.id = r.fixture_id"
        " WHERE r.run_id=? AND r.strategy=?"
        " ORDER BY r.fixture_id", (run_id, STRATEGY)).fetchall()
    league_set = set(leagues)
    for row in rows:
        fid, league = row["fixture_id"], row["league"]
        if fid in seen or league not in league_set:
            seen.add(fid)                        # 见过即记（无论调没调）
            continue
        seen.add(fid)
        called += 1
        try:
            persona_md = persona_path(league).read_text(encoding="utf-8")
            prompt = build_prompt(persona_md, build_input(conn, fid))
            output = extract_json(call_hermes(prompt))
            validate_output(output)
            apply_verdict(conn, fid, output)
        except FileNotFoundError:
            degraded.append({"fixture_id": fid, "reason": "persona_file"})
            continue
        except PersonaError as exc:
            reason = getattr(exc, "reason", "unknown")
            degraded.append({"fixture_id": fid,
                             "reason": _REASON.get(reason, reason)})
            continue
        ok += 1
        veto += 1 if output["verdict"] == "veto" else 0
    conn.commit()
    return {"called": called, "ok": ok, "veto": veto,
            "degraded": degraded, "attempted": sorted(seen)}
```

- [ ] **Step 4: 跑测确认通过**

Run: `uv run pytest tests/persona/test_apply.py -v`
Expected: 5 PASS

- [ ] **Step 5: Commit**

```bash
git add src/fa/persona/apply.py tests/persona/test_apply.py
git commit -m "feat(persona): §6.4 判决广播（veto 置零）与阶段编排（场次级降级/attempted）"
```

---

### Task 9: personas/bundesliga.md 打样【执行到本任务后暂停——等负责人确认风格再继续 Task 10】

**Files:**
- Create: `personas/bundesliga.md`

**Interfaces:**
- Produces: 德甲人格文件（Task 8 的 `persona_path("D1")` 读它；§6.2 prompt = 全文 + 输入 + 契约说明，文件内不内嵌输出契约）

- [ ] **Step 1: 写入打样文件**（全文如下）

```markdown
# 德甲人格 —— 老凯

## 你是谁

你是「老凯」，54 岁，凯泽斯劳滕人，看了 37 年德甲。给 kicker 写了 20 年专栏，
现在半退休。你的判断习惯：先看证据再开口，宁可不说话也不说错话。你讨厌
夸张修辞，一个「主力中场停赛」的信息你只会写一句，不会写成一段。

## 你的知识域（用 web_search 查证，别凭记忆报伤停）

- 伤停与停赛：官方比赛日名单、俱乐部公告；鲁尔区德比与国家德比的情绪放大要打折
- 赛程密度：周中有欧战的队（拜仁、勒沃库森、多特、莱比锡为主）周末轮换概率高，
  特别是国际比赛日回来的第一个周末
- 冬歇期（12 月中–1 月中）：前后两轮的状态断档，客队长途奔波影响放大
- 保级与欧战动机：赛季末段 15–17 名的求生欲、已无缘欧战的队无欲无求
- 50+1 与俱乐部治理：管理层动荡（体育总监离职、教练下课传闻）是明确的利空信号
- 升班马（赛季初 3 支）：主场气氛加成真实存在，但阵容深度在 12 月后崩

## 判断纪律（必须遵守）

1. 你只消费「本场输入」JSON 与 web_search 结果；**不得发明任何数字或伤病**
2. 伤停以官方名单或 kicker/图片报等权威来源为准；小报转会传闻不算证据
3. 查不到可靠增量信息 → verdict 用 agree（模型为先：你没有增量就不该动手）
4. downweight 只用于有明确证据的利空：主力伤停 2 人以上、三天两赛、更衣室动荡
5. veto 极少用：只有压倒性证据（核心三人以上缺阵、教练赛前一天下课）才否决
6. confidence_delta 的绝对值从 0.05 起步，重大利空才到 0.10–0.15——小幅、诚实
7. report_md 中文，先结论后依据，两三句话说完，不写客套与免责声明
```

- [ ] **Step 2: 冒烟——真 hermes 跑一次打样文件**（验收人格服从性，ark 额度可忽略）

```bash
uv run python -c "
import sqlite3, json
from fa.db import connect
from fa.config import persona_path
from fa.persona.contract import build_prompt, extract_json, validate_output
from fa.persona.caller import call_hermes
conn = connect('data/fa.db')          # 无库时改用最小手写 dict 走 build_prompt
obj = {'league': 'D1', 'match': {'kickoff_utc': '2026-09-12T13:30:00Z',
      'home': 'Bayern Munich', 'away': 'Borussia Dortmund'},
      'candidates': [{'market': 'H', 'model_p': 0.52, 'market_p': 0.48,
      'best_odds': 1.95, 'edge': 0.04, 'ev': 0.014, 'kelly_stake_frac': 0.008}],
      'model_summary': {'p_home': 0.52}, 'form': {'home_last5': ['W','W','D','L','W'],
      'away_last5': ['D','L','W','W','L']}, 'h2h_recent': []}
prompt = build_prompt(persona_path('D1').read_text(), obj)
out = extract_json(call_hermes(prompt)); validate_output(out)
print(json.dumps(out, ensure_ascii=False, indent=2))"
```

Expected: 合法 JSON（verdict 三值之一），key_factors 中文 ≤50 字/条——输出贴给负责人。

- [ ] **Step 3: Commit 后暂停等确认**

```bash
git add personas/bundesliga.md
git commit -m "feat(personas): 德甲人格「老凯」打样（待负责人确认风格）"
```

**→ 暂停点：把冒烟输出与文件全文呈给负责人；风格确认后才能开始 Task 10。**

---

### Task 10: personas 其余四文件铺开

**Files:**
- Create: `personas/epl.md`、`personas/laliga.md`、`personas/seriea.md`、`personas/ligue1.md`

**Interfaces:**
- Consumes: Task 9 已确认的 bundesliga.md 骨架（你是谁/知识域/判断纪律七条——**纪律七条逐字保留**，人格与知识域按下面要点分化）
- Produces: 五联赛人格齐备（`persona_path` 全映射可用）

- [ ] **Step 1: 四文件按骨架写入**（人格与知识域要点如下，语言风格与打样一致）

| 文件 | 人格设定 | 知识域特有点（在通用七条纪律之外补充的联赛特异性） |
|---|---|---|
| epl.md | 「薇拉」，42 岁伦敦记者，英超节奏与身体对抗的信徒，看过每一场北伦敦德比 | 圣诞赛程（12 月 26 日起 3 轮/周）体能崩塌；Big6 欧战轮换；保级队换帅反弹；VAR 与红牌上诉结果；冬窗豪购的磨合期 |
| laliga.md | 「马诺罗」，58 岁塞维利亚酒吧老板，青训与战术纪律的行家 | 欧战周中的传控体能税；巴萨/皇马国家德比的情绪；塞维利亚/贝蒂斯德比；保级三队 1 月后的借款买人；裁判尺度对技术流的主场偏向 |
| seriea.md | 「焦尔乔」，49 岁都灵体育报记者，链式防守美学与更衣室政治的观察者 | 战术克制（强队互蹲）；换帅如换刀的意大利传统；欧战意军团的轮换；南方球队主场（那不勒斯/巴勒莫式氛围）；伤病管理文化（轮换中卫） |
| ligue1.md | 「克莱尔」，38 岁里昂球探出身，青训流水线与非洲杯（1 月）缺席效应专家 | 非洲杯/亚洲杯 1–2 月抽调（塞内加尔/科特迪瓦国脚多的队伤筋动骨）；巴黎一队独走的市场失真；欧战小球队双线崩盘；降级三队的财政动机 |

每个文件的「判断纪律」节 = bundesliga.md 的七条**逐字复制**（纪律是跨联赛的人格不变量）。

- [ ] **Step 2: 冒烟四联**（Task 9 Step 2 的脚本把 `'D1'`/队名换为对应联赛各跑一次，输入用各联赛真实候选或手写 dict）

Run: 逐联赛跑，Expected: 四次全部合法 JSON

- [ ] **Step 3: Commit**

```bash
git add personas/
git commit -m "feat(personas): E0/SP1/I1/F1 四联赛人格（纪律七条同源，知识域分化）"
```

---

### Task 11: value 同刻双落（A1）

**Files:**
- Modify: `src/fa/pipeline/value.py`（`STRATEGY` → `STRATEGIES`、`_upsert_recommendation` 参数化 strategy、`generate_recommendations` 双写）
- Test: `tests/pipeline/test_value.py`（追加；既有用例不动应仍绿——它们断言 model_only 行为）

**Interfaces:**
- Produces:
  - `STRATEGIES = ("model_only", "model_persona")`（A/B 单源，paper/render/cli 引用）
  - `generate_recommendations(...)` 语义变化：每个过门槛候选写**两行**（同数字）；model_persona 行 `final_stake_frac=kelly_stake_frac`（中性初始）、`verdict`/`confidence_delta`/`key_factors`/`report_md` 为 NULL；返回 ids 含两轨
  - `_UNPLACED_SQL` 消费方注意：recommendations 行数翻倍

- [ ] **Step 1: 失败测试**（追加；种子复用 test_value 既有合成库夹具——反推盘口那套照用）

```python
def test_generate_writes_both_tracks_single_commit(conn_seeded, clock):
    ids = generate_recommendations(conn_seeded, ["E0"], "am", RUN_ID)
    rows = conn_seeded.execute(
        "SELECT strategy, market, kelly_stake_frac, final_stake_frac, verdict"
        " FROM recommendations WHERE run_id=? ORDER BY strategy, market",
        (RUN_ID,)).fetchall()
    assert len(rows) % 2 == 0
    mo = [r for r in rows if r["strategy"] == "model_only"]
    mp = [r for r in rows if r["strategy"] == "model_persona"]
    assert len(mo) == len(mp) > 0
    for a, b in zip(mo, mp):                     # 同数字、两轨
        assert a["market"] == b["market"]
        assert a["kelly_stake_frac"] == b["kelly_stake_frac"]
    for r in mp:                                 # persona 轨中性初始
        assert r["final_stake_frac"] == r["kelly_stake_frac"]
        assert r["verdict"] is None


def test_rerun_refreshes_prices_keeps_verdict(conn_seeded, clock):
    generate_recommendations(conn_seeded, ["E0"], "am", RUN_ID)
    conn_seeded.execute(
        "UPDATE recommendations SET verdict='veto', confidence_delta=0,"
        " final_stake_frac=0 WHERE strategy='model_persona'")
    conn_seeded.commit()
    generate_recommendations(conn_seeded, ["E0"], "am", RUN_ID)   # pm 重跑语义
    row = conn_seeded.execute(
        "SELECT verdict, final_stake_frac, kelly_stake_frac FROM recommendations"
        " WHERE strategy='model_persona' ORDER BY id").fetchone()
    assert row["verdict"] == "veto"              # 判决三列不清（M3 接缝语义）
    assert row["final_stake_frac"] == 0.0
```

- [ ] **Step 2: 跑测确认失败** → FAIL（只有单轨行）

Run: `uv run pytest tests/pipeline/test_value.py -v -k track`

- [ ] **Step 3: 实现**（value.py 三处）

```python
STRATEGIES = ("model_only", "model_persona")   # §6.6 A/B 双轨（单源，paper/render 引用）
```

`_upsert_recommendation` 加 `strategy: str` 参数（INSERT/SELECT 的 STRATEGY 换形参）；`generate_recommendations` 内层落库处：

```python
            for strategy in STRATEGIES:
                ids.append(_upsert_recommendation(
                    conn, run_id=run_id, fixture_id=fixture["id"], market=market,
                    phase=phase, strategy=strategy, model_p=p, market_p=market_p,
                    best_odds=odds, bookmaker=bookmaker, edge=edge, ev=ev,
                    kelly=kelly, created_at=_iso(now),
                    final=kelly if strategy == "model_persona" else None))
```

`_upsert_recommendation` 的 INSERT 列加 `final_stake_frac`（model_only 落 NULL 维持 M3 语义「退回 kelly」，model_persona 落 kelly 中性）；DO UPDATE 的 SET 列**不含** final_stake_frac（同三列不清）。

- [ ] **Step 4: 跑测 + 全量回归**

Run: `uv run pytest tests/pipeline/test_value.py tests/pipeline/test_paper.py tests/pipeline/test_matchday.py -v`
Expected: PASS（matchday/paper 既有断言若因行数翻倍失败，属预期——本步只允许修断言里的计数语义，不改生产行为；matchday 编排在 Task 13 消化双轨）

- [ ] **Step 5: Commit**

```bash
git add src/fa/pipeline/value.py tests/pipeline/test_value.py
git commit -m "feat(value): 同刻双落 model_only/model_persona（中性初始，刷新不清判决）"
```

---

### Task 12: paper 分轨 bankroll（D2）

**Files:**
- Modify: `src/fa/pipeline/paper.py`（`BANKROLL_KEY` → `bankroll_key()`+迁移；place/settle/summary 分轨）
- Test: `tests/pipeline/test_paper.py`（追加）

**Interfaces:**
- Consumes: `fa.pipeline.value.STRATEGIES`（Task 11 单源）
- Produces:
  - `LEGACY_BANKROLL_KEY = "paper_bankroll"`（仅迁移用）
  - `bankroll_key(strategy: str) -> str` → `"paper_bankroll:{strategy}"`
  - `ensure_bankroll_migrated(conn) -> None`——旧键存在 → 复制为 `:model_only` 同值 → 删旧键；幂等；不 commit（随外层）；place/settle/summary 入口都调
  - `place_paper_bets(conn, run_id) -> int`——双轨循环：各查各轨未落推荐（`_UNPLACED_SQL` 参数化 strategy）、各读各轨 bankroll（缺失惰性初始化 1000）；`final_stake_frac <= 0`（veto）跳过不落注；NULL → 退 kelly（M3 语义）
  - `settle_paper_bets(conn) -> dict`——pnl 按 `recommendations.strategy` 分轨累计、各轨独立回写；返回分轨 `{"by_strategy": {s: {"settled","won","pnl","clv_median"}}, "settled","won","pnl","clv_median"}`（顶层为合计，daily 简报用）
  - `paper_summary(conn) -> dict[str, dict]`——键 = strategy（`STRATEGIES` 序），值 = 原形状 dict（n/staked/returned/roi/pending/bankroll/clv_median，全按轨）

- [ ] **Step 1: 失败测试**（追加；数字先验算——质量清单 #3：两轨各落 1 注 kelly=0.01 × bankroll 1000 = 10.00；结算 1 中 1 负 → model_only 轨 pnl = 10×2.0−10 = +10.0、model_persona 轨 pnl = −其 stake）

```python
def test_bankroll_key_shape():
    assert bankroll_key("model_only") == "paper_bankroll:model_only"
    assert bankroll_key("model_persona") == "paper_bankroll:model_persona"


def test_migration_copies_legacy_to_model_only(conn):
    set_meta(conn, "paper_bankroll", "990.5"); conn.commit()
    ensure_bankroll_migrated(conn); conn.commit()
    assert get_meta(conn, "paper_bankroll:model_only") == "990.5"
    assert get_meta(conn, "paper_bankroll") is None       # 旧键删除，无双源
    ensure_bankroll_migrated(conn); conn.commit()          # 幂等
    assert get_meta(conn, "paper_bankroll:model_only") == "990.5"


def test_place_double_track_separate_bankrolls(conn_two_recs):
    # 种子：同 fixture 同 market 两轨各一行，final 均为 0.01（中性）
    placed = place_paper_bets(conn_two_recs, RUN_ID)
    assert placed == 2
    stakes = conn_two_recs.execute(
        "SELECT b.stake, r.strategy FROM bets b JOIN recommendations r"
        " ON r.id=b.recommendation_id").fetchall()
    assert all(s["stake"] == 10.0 for s in stakes)         # 各轨 1000 × 0.01
    assert get_meta(conn_two_recs, "paper_bankroll:model_only") == "1000.0"
    assert get_meta(conn_two_recs, "paper_bankroll:model_persona") == "1000.0"


def test_place_skips_veto(conn_veto):
    placed = place_paper_bets(conn_veto, RUN_ID)           # persona 轨 veto 行
    assert placed == 1                                     # 只落 model_only
    row = conn_veto.execute(
        "SELECT r.strategy FROM bets b JOIN recommendations r"
        " ON r.id=b.recommendation_id").fetchone()
    assert row["strategy"] == "model_only"


def test_settle_writes_per_strategy_bankroll(conn_played):
    out = settle_paper_bets(conn_played)
    # 种子断言在 docstring 写明：model_only 注中（odds 2.0, stake 10）、
    # model_persona 注负（stake 10）→ 前者 bankroll 1010.0、后者 990.0
    assert get_meta(conn_played, "paper_bankroll:model_only") == "1010.0"
    assert get_meta(conn_played, "paper_bankroll:model_persona") == "990.0"
    assert out["by_strategy"]["model_only"]["pnl"] == 10.0
    assert out["pnl"] == 0.0                               # 顶层合计


def test_summary_by_strategy(conn_played):
    s = paper_summary(conn_played)
    assert set(s) == {"model_only", "model_persona"}
    assert s["model_only"]["roi"] is not None
    assert s["model_persona"]["n"] == 1
```

（`conn_two_recs`/`conn_veto`/`conn_played` 种子照文件头部既有辅助构造。）

- [ ] **Step 2: 跑测确认失败** → FAIL（`bankroll_key` 未定义）

- [ ] **Step 3: 实现**（paper.py 改造要点）

```python
from fa.pipeline.value import STRATEGIES

LEGACY_BANKROLL_KEY = "paper_bankroll"     # M3 单键，仅迁移读（D2）
MODE = "paper"


def bankroll_key(strategy: str) -> str:
    return f"paper_bankroll:{strategy}"


def ensure_bankroll_migrated(conn: sqlite3.Connection) -> None:
    """旧单键 → model_only 轨（M3 的 14 注全 model_only，归属无歧义）；幂等。"""
    legacy = get_meta(conn, LEGACY_BANKROLL_KEY)
    if legacy is not None and get_meta(conn, bankroll_key("model_only")) is None:
        set_meta(conn, bankroll_key("model_only"), legacy)
    if legacy is not None:
        conn.execute("DELETE FROM meta WHERE key=?", (LEGACY_BANKROLL_KEY,))
```

`_bankroll(conn, strategy)`、`_UNPLACED_SQL` 加 `r.strategy=?` 形参化；`place_paper_bets` 外层 `for strategy in STRATEGIES:`（veto 跳过：`if frac is not None and frac <= 0: continue`；`frac is None → kelly` 保留）；`settle_paper_bets` 按 `r.strategy` 分组累计 `pnl_by`、循环后逐轨 `set_meta(bankroll_key(s), ...)`（结算前各轨只读一次）；`paper_summary` 逐轨查询返回 dict（每轨复用现有 SQL 加 `AND r.strategy=?` 的 join 版）——place/settle/summary 首行都调 `ensure_bankroll_migrated(conn)`。

- [ ] **Step 4: 跑测 + 回归**

Run: `uv run pytest tests/pipeline/test_paper.py tests/pipeline/test_matchday.py -v`
Expected: PASS（matchday 既有用例若因注数语义变化失败，计数断言随 Task 13 统一消化；本任务结束时 test_paper 全绿）

- [ ] **Step 5: Commit**

```bash
git add src/fa/pipeline/paper.py tests/pipeline/test_paper.py
git commit -m "feat(paper): bankroll 按 strategy 分轨（迁移/双轨落注/veto 跳过/分轨结算）"
```

---

### Task 13: matchday 集成——am 插层 + pm 补跑

**Files:**
- Modify: `src/fa/pipeline/matchday.py`（`_run` 插 persona 阶段；`summary["persona"]`）
- Test: `tests/pipeline/test_matchday.py`（追加）

**Interfaces:**
- Consumes: `run_persona_phase`（Task 8）；`generate_recommendations`/`place_paper_bets`（Task 11/12 双轨语义）
- Produces:
  - `_run` 顺序：value → **persona** → paper → render → send（am/pm 同位）
  - am：`run_persona_phase(conn, run_id, leagues)`（全量候选）；pm：`run_persona_phase(conn, run_id, leagues, attempted=_am_attempted(conn, am_run_id))`；`am_run_id is None`（无对照）时 `attempted=None` 全跑
  - `summary["persona"] = persona_summary`（runs.summary 落库——pm 的 `_am_attempted` 数据源）
  - `_am_attempted(conn, am_run_id) -> set[int]`——读 am run 的 `runs.summary` JSON 的 `persona.attempted`；解析失败/缺键 → 空 set（fail-open：pm 多跑无害，漏跑有害）
  - 返回 dict 增加 `"persona": {"called", "ok", "veto", "degraded"}`（attempted 不进 CLI 返回，只进 summary）

- [ ] **Step 1: 失败测试**（追加；三层替身同文件既有模式——fetch_odds 回放、render/send 记录器、`HERMES_BIN` 指向 fixture）

```python
def test_am_runs_persona_and_records_summary(monkeypatch, ...):
    monkeypatch.setenv("HERMES_BIN", str(FIX / "hermes_ok"))
    result = run_matchday(c, "am", ["E0"])
    summary = json.loads(c.execute(
        "SELECT summary FROM runs WHERE id=?", (result["run_id"],)).fetchone()[0])
    assert summary["persona"]["called"] >= 1
    assert summary["persona"]["ok"] == summary["persona"]["called"]
    assert result["persona"]["veto"] == 0
    row = c.execute("SELECT verdict FROM recommendations"
                    " WHERE strategy='model_persona'").fetchone()
    assert row["verdict"] == "agree"               # 判决已落库（渲染可读）


def test_pm_reuses_am_verdicts(monkeypatch, ...):
    monkeypatch.setenv("HERMES_BIN", str(FIX / "hermes_ok"))
    am = run_matchday(c, "am", ["E0"])
    calls = []
    monkeypatch.setattr("fa.pipeline.matchday.run_persona_phase",
                        lambda *a, **k: calls.append(a) or _PHASE_STUB)
    pm = run_matchday(c, "pm", ["E0"])
    attempted = calls[0][3]                        # 第四位置参
    assert attempted                               # 非空：am 的 attempted 传入
    assert set(attempted) == {r[0] for r in c.execute(
        "SELECT DISTINCT fixture_id FROM recommendations WHERE run_id=?"
        " AND strategy='model_persona'", (am["run_id"],))}


def test_persona_degradation_never_fails_run(monkeypatch, ...):
    monkeypatch.setenv("HERMES_BIN", str(FIX / "hermes_timeout"))
    monkeypatch.setenv("FA_PERSONA_TIMEOUT", "0.5")
    result = run_matchday(c, "am", ["E0"])
    assert result["status"] in ("ok", "degraded_ok")   # §6.5 降级不中断
    assert result["persona"]["called"] > 0
    assert result["persona"]["ok"] == 0
    assert result["persona"]["degraded"][0]["reason"] == "timeout"
```

（`...` 为该文件既有夹具签名；`_PHASE_STUB` = `{"called": 0, "ok": 0, "veto": 0, "degraded": [], "attempted": []}`。）

- [ ] **Step 2: 跑测确认失败** → FAIL（无 persona 阶段/返回键）

- [ ] **Step 3: 实现**（matchday.py）

```python
from fa.persona.apply import run_persona_phase
```

`_run` 内、`rec_ids = generate_recommendations(...)` 之后：

```python
    attempted = (_am_attempted(conn, am_run_id)
                 if phase == "pm" and am_run_id is not None else None)
    persona_summary = run_persona_phase(conn, run_id, leagues, attempted)
    placed = place_paper_bets(conn, run_id)
```

`summary` dict 加 `"persona": persona_summary`；`_result(...)` 加 `persona={"called": persona_summary["called"], "ok": persona_summary["ok"], "veto": persona_summary["veto"], "degraded": len(persona_summary["degraded"])}`；新增：

```python
def _am_attempted(conn: sqlite3.Connection, am_run_id: int) -> set[int]:
    """am run summary 里的 persona.attempted（pm 沿用判决的依据，§6.2）。
    解析失败/缺键 → 空 set：fail-open（pm 多跑一次无害，漏判有害）。"""
    row = conn.execute("SELECT summary FROM runs WHERE id=?",
                       (am_run_id,)).fetchone()
    if row is None or not row["summary"]:
        return set()
    try:
        parsed = json.loads(row["summary"])
        return set(parsed.get("persona", {}).get("attempted", []))
    except (ValueError, AttributeError, TypeError):
        return set()
```

（matchday.py 头部补 `import json`——若未导入。）

- [ ] **Step 4: 跑测 + 全量回归**

Run: `uv run pytest tests/pipeline/test_matchday.py -v && uv run pytest -q`
Expected: PASS（全量绿；既有用例的注数/行数断言在本任务一并改为双轨口径）

- [ ] **Step 5: Commit**

```bash
git add src/fa/pipeline/matchday.py tests/pipeline/test_matchday.py
git commit -m "feat(matchday): value→persona→bet 插层，pm 沿用 am 判决只补新增场次"
```

---

### Task 14: render persona 段 + fa status 双行

**Files:**
- Modify: `src/fa/report/render.py`（`_persona_block` 真渲染、bankroll 块分轨、pm diff 行判决标识）、`src/fa/cli.py`（`_b_line_summary` 两轨）
- Test: `tests/report/test_render.py`（追加）、`tests/test_cli.py`（追加）

**Interfaces:**
- Consumes: `bankroll_key`/`STRATEGIES`（Task 12/11）；`recommendations.verdict/key_factors/report_md`（Task 4/8）；`summary["persona"]["degraded"]`（Task 13）
- Produces:
  - am 报告 persona 段：该 run 的 model_persona 判决行按**场次**渲染（✅ agree / ⚠️ downweight（delta）/ ⛔ veto）+ key_factors 缩进逐条 + report_md 截 200 字引用行；降级行「⚪ …：persona 未生效（reason）」；段尾汇总「persona N 场：✅a ⚠️b ⛔c · 未生效 d」；零判决 →「- 本相位无 persona 判决」
  - pm 更新版：新增候选行尾附判决标识；盘口移动行不带点评
  - bankroll 块：两轨各一行（`- model_only：1000.00` / `- model_persona：987.50`）
  - `fa status` B 线栏：`[model_only] …` / `[model_persona] …` 两行（格式同现有单行）

- [ ] **Step 1: 失败测试**（render 侧；种子 = 判决行 + 降级 summary）

```python
def test_am_report_persona_block_with_all_verdicts(conn_seeded):
    text = render_matchday_report(conn_seeded, RUN_ID, "am", SUMMARY, 480, False)
    assert "✅" in text and "⚠️" in text and "⛔" in text
    assert "persona 3 场：✅1 ⚠️1 ⛔1 · 未生效 1" in text
    assert "persona 未生效（timeout）" in text
    assert len([l for l in text.splitlines()
                if l.strip().startswith("> ")]) >= 1     # report_md 引用行


def test_am_report_truncates_report_md_200(conn_seeded):
    # 种子 report_md = "字" * 300 → 渲染后单行 ≤ 202 字符（> + 200 字 + 无省略号也行，断言 ≤ 203）
    text = render_matchday_report(conn_seeded, RUN_ID, "am", SUMMARY, 480, False)
    md_lines = [l for l in text.splitlines() if l.startswith("  > ")]
    assert md_lines and len(md_lines[0]) <= 4 + 200


def test_bankroll_block_two_tracks(conn_seeded):
    text = render_matchday_report(conn_seeded, RUN_ID, "am", SUMMARY, 480, False)
    assert "- model_only：" in text and "- model_persona：" in text
```

（cli 侧：`test_cli.py` 追加一例断言 `fa status` 输出含 `[model_only]` 与 `[model_persona]` 两行——库用 tmp 种子。）

- [ ] **Step 2: 跑测确认失败** → FAIL（占位文本「persona 未接入（M4）」仍在）

- [ ] **Step 3: 实现**（render.py 要点）

```python
from fa.pipeline.paper import bankroll_key          # 替换 BANKROLL_KEY import
from fa.pipeline.value import STRATEGIES

_VERDICT_ICON = {"agree": "✅", "downweight": "⚠️", "veto": "⛔"}
_REASON_CN = {"timeout": "超时", "exit": "hermes 异常退出", "extract": "输出无法解析为 JSON",
              "contract": "输出不合规", "persona_file": "人格文件缺失", "unknown": "未知"}


def _persona_block(conn, run_id, summary):
    rows = conn.execute(
        "SELECT r.fixture_id, r.verdict, r.confidence_delta, r.key_factors,"
        " r.report_md, f.event_key, th.name AS home, ta.name AS away"
        " FROM recommendations r"
        " JOIN fixtures f ON f.id=r.fixture_id"
        " LEFT JOIN teams th ON th.id=f.home_team_id"
        " LEFT JOIN teams ta ON ta.id=f.away_team_id"
        " WHERE r.run_id=? AND r.strategy='model_persona' AND r.verdict IS NOT NULL"
        " GROUP BY r.fixture_id ORDER BY f.kickoff_utc, r.fixture_id",
        (run_id,)).fetchall()
    degraded = ((summary or {}).get("persona") or {}).get("degraded") or []
    counts = {"agree": 0, "downweight": 0, "veto": 0}
    lines = ["## persona", ""]
    for r in rows:                                   # 判决行（场次级）
        counts[r["verdict"]] += 1
        delta = ("" if r["verdict"] in ("agree", "veto")
                 else f"（{r['confidence_delta']:+.2f}）")
        lines.append(f"- {_VERDICT_ICON[r['verdict']]} {_label(r)} "
                     f"{r['verdict']}{delta}")
        for factor in json.loads(r["key_factors"] or "[]"):
            lines.append(f"  - {factor}")
        if r["report_md"]:
            lines.append(f"  > {r['report_md'][:200]}")
    for d in degraded:                               # 降级行（§6.5 标注）
        row = conn.execute(
            "SELECT f.event_key, th.name AS home, ta.name AS away FROM fixtures f"
            " LEFT JOIN teams th ON th.id=f.home_team_id"
            " LEFT JOIN teams ta ON ta.id=f.away_team_id WHERE f.id=?",
            (d["fixture_id"],)).fetchone()
        lines.append(f"- ⚪ {_label(dict(row))}：persona 未生效"
                     f"（{_REASON_CN.get(d['reason'], d['reason'])}）")
    total = len(rows) + len(degraded)
    if total:
        lines.insert(2, f"persona {total} 场：✅{counts['agree']}"
                     f" ⚠️{counts['downweight']} ⛔{counts['veto']}"
                     f" · 未生效 {len(degraded)}")
    else:
        lines.append("- 本相位无 persona 判决")
    return lines
```

`render_matchday_report` 的 `_persona_block()` 调用改 `_persona_block(conn, run_id, summary)`；`render_pm_update` **签名不变**——pm 的降级信息用既有 `_run_summary(conn, pm_run_id)` 内部读 runs 表（persona 段调 `_persona_block(conn, pm_run_id, _run_summary(conn, pm_run_id))`）；bankroll 块：

```python
def _bankroll_lines(conn):
    out = []
    for s in STRATEGIES:
        raw = get_meta(conn, bankroll_key(s))
        out.append(f"- {s}：" + ("未初始化" if raw is None else f"{float(raw):.2f}"))
    return out
```

pm 的 added 行尾标识：渲染前建 `verdict_icon = {fixture_id: icon}`（pm run 的 model_persona 判决行），added 行模板加 `{verdict_icon.get(r['fixture_id'], '')}`。cli `_b_line_summary` 改为 `for s, summ in paper_summary(conn).items():` 两行 `[s] ...`（格式函数复用既有 `_money/_pct`）。

- [ ] **Step 4: 跑测 + 全量回归**

Run: `uv run pytest tests/report/ tests/test_cli.py -v && uv run pytest -q`
Expected: PASS 全量

- [ ] **Step 5: Commit**

```bash
git add src/fa/report/render.py src/fa/cli.py tests/report/ tests/test_cli.py
git commit -m "feat(report): persona 段真渲染（✅⚠️⛔⚪/200 字截断/汇总行）+ status 双轨行"
```

---

### Task 15: 实跑①——库内候选契约实测（零额度）

**Files:**
- Create: `.superpowers/sdd/2026-09-04-m4-persona/probe_real.py`（gitignore 目录，不入库——M3 T12 同款）
- Modify: `docs/m4-report.md`（§1 实测数字）

**Interfaces:**
- Consumes: 真库（见 Step 0）+ Task 5-10 全链
- Produces: 真实不合规率 / 降级率 / 判决分布 / 单场耗时（m4-report §1，spec §11 风险 #5 的实测数字）

- [ ] **Step 0: 库准备**——worktree `data/fa.db` 若无（gitignore）：从 M3 worktree `.claude/worktrees/worktree-m3-dual-track/data/fa.db` 拷贝（含 M3 的 14 推荐/9 候选场）；M3 worktree 已删则 `uv run fa data sync --leagues ...` 重拉（-20 credits，如实记账）

- [ ] **Step 1: 探针脚本**（逐场真调 hermes，统计四类结果；脚本落 `.superpowers/sdd/` 并打印逐场 verdict/耗时/失败类）

```python
"""实跑①：对库内真实候选逐场调 hermes -z，统计契约合规（gitignore，不入库）。"""
import json
import time

from fa.config import persona_path
from fa.db import connect
from fa.persona import PersonaError
from fa.persona.caller import call_hermes
from fa.persona.contract import (build_input, build_prompt, extract_json,
                                 validate_output)

conn = connect("data/fa.db")
rows = conn.execute(
    "SELECT DISTINCT r.fixture_id, r.run_id, f.league FROM recommendations r"
    " JOIN fixtures f ON f.id=r.fixture_id"
    " WHERE r.strategy='model_only' AND r.phase='am'"
    " ORDER BY r.fixture_id").fetchall()
stats = {"ok": 0, "timeout": 0, "exit": 0, "extract": 0, "contract": 0}
for row in rows:
    t0 = time.monotonic()
    try:
        prompt = build_prompt(persona_path(row["league"]).read_text(),
                              build_input(conn, row["fixture_id"]))
        out = extract_json(call_hermes(prompt))
        validate_output(out)
        stats["ok"] += 1
        verdict = out["verdict"]
    except PersonaError as e:                      # 覆盖 timeout/exit/extract/contract
        stats[getattr(e, "reason", "exit")] += 1
        verdict = f"DEGRADED:{getattr(e, 'reason', 'exit')}"
    print(f"fixture={row['fixture_id']} {row['league']} {verdict}"
          f" {time.monotonic()-t0:.1f}s", flush=True)
print(json.dumps(stats, ensure_ascii=False))
```

- [ ] **Step 2: 跑三遍**（LLM 非确定性 → 合规率取三遍观察，逐遍记录；不合规样本原文摘录进报告——诊断 persona 文件/契约说明的改进点）

Run: `uv run python .superpowers/sdd/2026-09-04-m4-persona/probe_real.py` ×3

- [ ] **Step 3: 数字写进 m4-report §1 并 Commit**（表：场 × 遍的 verdict 矩阵、四类失败计数、单场耗时 min/median/max；**不调参凑合规率**——若不合规率 >20% 如实写并给 persona 文件改进建议，不悄悄改文件重跑到好看为止）

```bash
git add docs/m4-report.md && git commit -m "docs: m4-report §1——实跑①契约合规实测（三遍矩阵）"
```

---

### Task 16: 实跑②——比赛日 E2E + 验收完稿

**Files:**
- Modify: `docs/m4-report.md`（完稿）、`CLAUDE.md`（状态行 M4 完成）

**Interfaces:**
- Consumes: 全链 + 真实比赛日 + 真实 `.env` key
- Produces: M4 验收结论（spec §10「mock + 实跑测试通过；A/B 数据落库」）

- [ ] **Step 1: 择比赛日跑完整 am**

前提：当日 52h 窗口内有五大联赛赛事（周六最稳）；`.env` 有 `ODDS_API_KEY`（余 ~460，本次 -20）；TG 推送须带 Clash 代理（2026-09-04 实测：直连 api.telegram.org 不通）——`export https_proxy=http://127.0.0.1:7890 http_proxy=http://127.0.0.1:7890`；代理离线时推送按设计降级（不中断 run），不是缺陷。

Run: `uv run fa run matchday --phase am`
Expected: `判决：ok`；CLI 摘要含 persona 行（called/ok/veto）

- [ ] **Step 2: 库内断言**（双轨行成对、persona 三列+两列已填或中性、双轨注挂对轨、分轨 bankroll 各就位）

```bash
sqlite3 data/fa.db "
SELECT strategy, COUNT(*), SUM(verdict IS NOT NULL) FROM recommendations
 WHERE run_id=(SELECT MAX(id) FROM runs WHERE type='matchday') GROUP BY strategy;"
sqlite3 data/fa.db "
SELECT r.strategy, COUNT(*), SUM(b.stake) FROM bets b
 JOIN recommendations r ON r.id=b.recommendation_id
 WHERE b.mode='paper' GROUP BY r.strategy;"
sqlite3 data/fa.db "SELECT key, value FROM meta WHERE key LIKE 'paper_bankroll%';"
```

Expected: 两轨行数相等；model_persona 轨已判行数 = persona called−degraded；veto 行无 bets；两轨 bankroll 键各一。

- [ ] **Step 3: 报告验收**——离线复渲染（M3 T12 同款：`render_matchday_report` 只读库）核对 §7.1-2/3：✅⚠️⛔ 标识、key_factors、200 字截断、降级标注、bankroll 两行、段尾汇总行

- [ ] **Step 4: 验收清单核对 + CLAUDE.md 状态更新 + 完稿**

对照设计文档 §8 清单逐项打勾（全量测试绿 / 实跑①数字 / 实跑②数字 / spec 修订落库 / personas 五文件入库）；CLAUDE.md 当前状态节加 M4 完成行（含实测数字）；`docs/m4-report.md` 完稿提交。

Run: `uv run pytest -q`（最后一次全量，数字进报告）
Expected: 全绿

```bash
git add docs/m4-report.md CLAUDE.md
git commit -m "docs: M4 验收报告完稿（实跑①②实测数字）+ CLAUDE.md 状态更新"
```

---

## 自审记录（writing-plans Self-Review）

1. **Spec 覆盖**：§6.1（白名单→T2 探针+T5 命令）、§6.2（调用方式/超时/pm 沿用→T5/T13）、§6.3（I/O→T6/T7）、§6.4（映射→T8）、§6.5（降级→T8/T13/T14）、§6.6（双轨→T11/T12）、§7.1-2/3（渲染→T14）、§7.2（双轨落注→T12）、§10 M4 验收（T15/T16）、§9.7 测试形态（T5 fixture 脚本）；设计文档 D1-D4/A1/B1/C1 全段落任务。**发现的 spec 盲点**：key_factors/report_md 无落库位（T4 schema v4 补）——设计文档「表结构就绪」判断有误，本计划修正
2. **占位符扫描**：Task 6/8/13 测试种子以语义注释 + 代表性断言给出（种子辅助沿用既有文件的构造模式，文件名/常量已逐一核对）——无「TBD/类似 Task N」
3. **类型一致性**：`build_command/call_hermes(prompt[, timeout])`、`build_input(conn, fixture_id)`、`extract_json(text)`、`validate_output(obj)`、`apply_verdict(conn, fixture_id, output)`、`run_persona_phase(conn, run_id, leagues, attempted)`、`bankroll_key(strategy)`、`paper_summary(conn)->dict[str,dict]`、`STRATEGIES` 单源（value 定义，paper/render 引用）——跨任务已核对
4. **质量清单**：#3 阈值先验算（T12 种子数字 10.0/1010.0/990.0 已验算）；#8 无先错后改双版本；#9 诚实条款在 Global Constraints 与 T15 Step 3 双写
