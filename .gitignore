# 密钥（spec §9.4）
.env

# 运行数据：SQLite 库与原始 CSV 缓存（spec §9.2）——仅根目录，避免误伤 src/fa/data 与 tests/data
/data/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/

# Claude Code 会话产物（worktrees 等）
.claude/
